# M365 公務車借用自動通知與後台管理流程 Master 專案紀錄

更新日期：2026-08-27
Master 狀態：本文件為目前唯一最新版本，後續功能分支應以本文件為基準。

## 一、目前專案總覽

| 項目 | 內容 |
|---|---|
| 專案名稱 | M365 公務車借用自動通知與後台管理流程 |
| 目前版本 | `v0.3.7` |
| 主要架構 | Outlook 資源行事曆 + Power Automate（Office 365 Outlook「傳送 HTTP 要求」直連 Graph）+ Teams Adaptive Card + SharePoint List |
| 授權限制 | O365 E1 可行，不使用 Power Automate Premium 連接器 |
| 目前開發階段 | 專案仍為**測試階段，尚未正式啟用**。2026-08-08～2026-08-27 期間逐項複查並拍板「差異確認決策清單」（`docs/company-account-and-booking-rules-change-decisions.md`）#128～#134 共 7 項：其中 #129/#130/#131/#132 複查後確認既有程式碼／架構已覆蓋，非待實作項目；#133（Graph 查詢時間窗調整為 -24hr～+14天）已完成並驗證寫入正式流程；#134（兩流程失敗告警）同步流程已確認、Teams 流程因 Power Automate 網頁本次連線異常尚待補查。複查過程中意外發現『公務車行事曆同步至SharePoint』流程已在未告知的情況下停用 19 天（8/8～8/27），已於 2026-08-27 經負責人同意重新開啟並手動執行一次補回同步資料。兩流程（同步流程、Teams 通知流程）目前狀態：同步流程已重新開啟，Teams 通知流程仍維持關閉，待 SG-T07 重新驗證（#137）與 Bug#1 端到端測試安排後再決定是否開啟；SG-T09 測試資料清理事項維持原狀 |
| 專案完成度 | 99%（#128～#134 決策清單項目除 #134 Teams 流程告警待補查外皆已結案；#135 Power Apps 即時同步可行性、#136 循環預約完整實作、#137 SG-T07 重新驗證仍待進行） |
| 下一個預計完成階段 | 依專案負責人指示續行 #135（Power Apps 即時同步可行性實測）、#136（循環預約完整實作）、#137（SG-T07／Teams 通知流程重新驗證）三項，並於 Power Automate 網頁恢復正常存取後補查 #134 Teams 流程告警設計 |

## 二、本次新增完成內容

### SharePoint

- 已在 `ALP_TW_AD` 建立 SharePoint List：`公務車借用管理`。
- 已建立並驗證主要欄位。
- 已新增 `是否整天` 欄位，用於支援 Outlook 整天預約。
- 已新增 `預計通知時間` 欄位，用於支援借用前通知規則。
- 已在正式清單新增並驗證系統防呆欄位，包含事件唯一識別、UTC 原始時間、事件同步狀態、卡片版本、通知狀態、Flow 執行鎖、重複資料檢查、取消與失效旗標。
- 已確認清單網址為 `https://alpglobal.sharepoint.com/sites/ALP_TW_AD/Lists/List6/AllItems.aspx`。

### Power Automate

- 已建立功能測試流程：`公務車功能測試-SharePoint清單連線`。
- 已確認 SharePoint 標準連接器可讀取 `公務車借用管理` 清單。
- 已確認 Office 365 Outlook 標準連接器基本連線可用。
- 已將正式流程拆分為兩條建議流程：行事曆同步、Teams 通知與回覆。
- 已建立並執行 `公務車功能測試-資源信箱讀取`（Flow ID：`7aff726f-0f9d-4b7e-a128-baeb368ab1ce`）。
- 已確認 Calendar ID 選單目前只有個人 `Calendar`；直接填入 Altis 資源信箱會回傳「ID 格式不正確」。
- 測試流程已關閉，保留執行紀錄供追蹤。

### Teams Adaptive Card

- 已完成卡片內容設計。
- 已納入固定版「公司公務車使用管理規範」文字。
- 已設計借用人必填欄位：共乘人數、規範確認。
- 已建立 Adaptive Card JSON 範例。

### Outlook

- 已確認三台公務車資源信箱資訊。
- 已確認 Outlook 標準連接器基本可用。
- 2026-07-02 已實測資源行事曆讀取；目前三台車未出現在 Power Automate Calendar ID 選單。
- 資源信箱 Email 不是 `取得事件的行事曆檢視 (V3)` 可接受的 Calendar ID。
- 已確認 `ad.general@alp.global` 具有三台資源信箱 Calendar Reviewer 權限，可在 Outlook 查看行事曆；但 V2 實測尚未列出三台共用行事曆。
- 本專案不修改 Resource Mailbox、Calendar Processing 或 Exchange 組態，因此不需要 Exchange Administrator。
- 已於 2026-07-02 15:34 成功執行 `取得行事曆 (V2)`，輸出只有個人 `Calendar`。
- 已於 2026-07-04 確認 Outlook 網頁可顯示三台公務車共用行事曆，但重新執行 `取得行事曆 (V2)` 仍只回傳一個個人 Calendar。
- 下一步需評估 Editor 權限或其他不使用 Premium 連接器的標準讀取方案。

### Excel

- 目前不使用 Excel 作為正式資料來源。
- 後台管理以 SharePoint List 為唯一主檔，避免多份資料造成維護成本。

### 通知流程

- 已新增通知時間規則：
  - 一般借用：借用起始時間前 1 小時通知。
  - 最早通知時間：當天早上 08:00。
  - 整天借用：當天早上 08:00。
- 已新增 `預計通知時間` 欄位，供 Power Automate 計算後寫回。

### 文件

- 已更新 `README.md`。
- 已更新 `CHANGELOG.md`。
- 已更新 SharePoint 欄位文件。
- 已更新 Power Automate 流程文件。
- 已更新 Teams Adaptive Card 文件。
- 已更新功能測試計畫。
- 已新增 Master 專案紀錄、里程碑、完成清單、待辦事項。

### 測試

- SharePoint List 欄位驗證完成。
- Power Automate SharePoint 連線測試完成。
- Power Automate Outlook 標準連接器基本測試完成。
- 通知時間支援欄位驗證完成。
- 資源行事曆 Calendar ID 可見性測試未通過，已確認為 M5 正式阻擋點。
- 資源信箱讀取測試流程已關閉，避免持續排程失敗。
- 已修正先前將 Calendar ID 錯誤推論為需要 Exchange Administrator 的判斷；既有 Reviewer 權限已足夠。
- 已確認 Codex 內建瀏覽器與 Chrome 均可作為 Power Automate 操作後端；後續應沿用目前已登入且已連線的工作階段，不得因舊版瀏覽器紀錄推定目前不可操作。實際失敗時須記錄當次工具授權或連線錯誤；詳見 `docs/calendar-access-test.md` 與 `docs/Project_Workflow.md`。

### 系統防呆設計

- 已將五項防呆機制列為正式上線前必須完成：
  - 統一時區為 `Asia/Taipei`。
  - 以 Event ID / iCalUId 作為唯一識別。
  - Outlook 取消與異動需同步 SharePoint。
  - 舊 Teams Adaptive Card 回覆前需重新檢查並可失效。
  - Flow 啟用執行鎖與重複資料防止。
- 已新增防呆設計文件：`docs/system-safeguards.md`。
- 已補強 SharePoint、Power Automate、Adaptive Card、測試案例文件。
- 已將防呆欄位實際建立於 SharePoint 正式清單，後續正式流程可直接寫入與更新。

## 三、目前各功能狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---:|---|
| 需求分析 | 已完成 | 100% | 核心需求、限制與例外情境已整理 |
| SharePoint List | 已完成 | 100% | 已在 `ALP_TW_AD` 建立並驗證 |
| Power Automate | 進行中 | 92% | P3 情境測試全數通過，事件取消偵測補強邏輯已驗證；「我的流程」清理、`是否測試資料` 正式化、Concurrency Control 皆已完成 |
| Adaptive Card | 設計完成 | 80% | JSON 範例完成，尚未正式串接發送 |
| Teams 通知 | 設計完成，待串接 | 30% | 通知時間規則已確認 |
| Outlook | 測試中 | 70% | ATA-9627 已驗證可讀取真實事件；Camry、Cross 待重複驗證 |
| 系統防呆設計 | 欄位已落地，流程待實作 | 82% | 五項防呆機制已文件化，SharePoint 欄位已建立 |
| 測試案例 | 進行中 | 65% | 已記錄 HTTP 直連 Graph 成功案例，防呆情境待實測 |
| 維護文件 | 已更新 | 95% | Master、治理、防呆、欄位與測試紀錄已更新 |
| 操作手冊 | 待建立 | 20% | 尚需整理行政人員日常操作手冊 |
| Git 版本 | 已建立 | 82% | 目前在 `feature/calendar-access-test` 分支，依 Project Governance 提交並同步 GitHub |

## 四、本次異動摘要

### 新增功能

- 新增 SharePoint 欄位 `是否整天`。
- 新增 SharePoint 欄位 `預計通知時間`。
- 新增通知時間規則：最早 08:00，整天借用 08:00。
- 新增 Master 專案紀錄文件。
- 新增里程碑、完成清單、待辦事項文件。
- 新增系統防呆設計文件與五項正式上線前必做防呆機制。
- 新增 SharePoint 正式清單防呆欄位：`iCalUId`、`唯一識別來源`、`事件最後修改時間`、`原始開始時間 UTC`、`原始結束時間 UTC`、`顯示時區`、`事件同步狀態`、`取消時間`、`卡片版本`、`卡片狀態`、`最新通知批次 ID`、`通知狀態`、`Flow 執行 ID`、`處理鎖定狀態`、`處理鎖定時間`、`重複資料檢查結果`、`防呆檢查結果`、`已取消`、`已失效`。

### 修改功能

- 專案 SharePoint 建立位置統一改為 `ALP_TW_AD`。
- 通知邏輯由單純「借用前 1 小時」改為「借用前 1 小時，但最早 08:00」。
- 整天借用通知時間改為固定當天 08:00。
- SharePoint 後台新增欄位以支援正式通知判斷。

### 修正 Bug

- 修正文檔亂碼問題。
- 修正早上過早借用時，通知時間可能落在上班前的流程設計問題。
- 修正整天借用沒有明確通知時間的規則缺口。

### 新增文件

- `docs/project-master-record.md`
- `docs/project-milestones.md`
- `docs/completion-checklist.md`
- `docs/todo.md`
- `release-notes/v0.2.0.md`
- `docs/Exchange_Resource_Mailbox_Permission.md`
- `release-notes/v0.2.4-resource-mailbox-access-check.md`

### 更新文件

- `README.md`
- `CHANGELOG.md`
- `sharepoint/list-schema.md`
- `power-automate/README.md`
- `adaptive-cards/README.md`
- `test-cases/function-test-plan.md`

## 五、下一階段工作

| 優先順序 | 工作項目 | 說明 |
|---:|---|---|
| 1 | [已完成] 解除 Resource Calendar 讀取阻擋 | 已改用「傳送 HTTP 要求」+ `calendar/events` 直連 Graph，ATA-9627 驗證成功，不需 Calendar ID |
| 2 | 對 Camry、Cross 重複驗證 | 沿用 ATA-9627 相同的 HTTP 要求路徑，逐台確認可讀取事件 |
| 3 | 建立正式行事曆同步流程 | 將 Outlook 預約同步到 SharePoint List，並寫入已建立的防呆欄位 |
| 4 | 建立取消與異動同步 | 行事曆取消、時間、車輛、主旨或借用人異動時同步更新 |
| 5 | 建立通知時間計算 | 依 `Asia/Taipei`、`是否整天`、`借用起始時間` 計算 `預計通知時間` |
| 6 | 建立 Teams Adaptive Card 發送流程 | 到達 `預計通知時間` 後發送給借用人，並避免重複通知 |
| 7 | 建立回覆前防呆檢查 | 檢查 Event ID、卡片版本、事件狀態，避免舊卡片覆蓋資料 |
| 8 | 建立 Flow 執行鎖 | 啟用 Concurrency Control，避免重複資料與 Race Condition |
| 9 | 執行端到端與防呆測試 | 從 Outlook 預約到 Teams 回覆到 SharePoint 後台確認 |
| 10 | 建立操作手冊 | 提供行政、總務人員日常使用與異常處理 |
| 11 | 上線前驗收 | 五項防呆機制與所有端到端測試通過後才可上線 |

## 六、Master 專案紀錄更新狀態

| 項目 | 狀態 |
|---|---|
| README | 已更新 |
| CHANGELOG | 已更新 |
| 專案里程碑 | 已更新 |
| 完成清單 | 已更新 |
| 待辦事項 | 已更新 |

## 七、目前判斷

本專案目前已完成後台資料結構、系統防呆欄位、基礎連線測試，並已解除資源行事曆存取閘門阻擋。SharePoint 與 Outlook 連接器驗證正常，ATA-9627 已可透過 Power Automate 正式讀取真實借用事件。

目前不需要以 Exchange Administrator 登入或執行流程。`ad.general@alp.global` 已具備三台資源信箱 Mailbox Full Access 權限；`取得行事曆 (V2)` 與 `取得事件的行事曆檢視 (V3)` 兩個動作經證實無法讀取 Resource Mailbox（屬連接器限制，非權限問題），已改用 Office 365 Outlook 標準連接器內建的「傳送 HTTP 要求」動作直連 Graph（`GET /v1.0/users/{mailbox}/calendar/events?$filter=...`），不需要 Calendar ID、不需要 Premium 授權。ATA-9627 已驗證成功，下一步為 Camry、Cross 重複驗證，並建立正式 SharePoint 同步流程。
## v0.2.7 Master 更新 - ATA-9627 V3 事件讀取測試

更新日期：2026-07-14

目前版本更新為 `v0.2.7`，本階段為 Resource Mailbox 事件讀取驗證階段。

### 本次新增完成內容

- 紀錄 ATA-9627 Resource Mailbox 的 Outlook Editor 權限已由 IT 設定完成。
- 紀錄 `ad.general@alp.global` 已可在 Outlook 端看到、建立、修改、刪除 ATA-9627 行事曆事件。
- 紀錄 Power Automate `取得行事曆 (V2)` 雖執行成功，但僅回傳 `ad.general@alp.global` 自身 Calendar。
- 新增下一階段 V3 事件讀取測試流程設計。
- 新增 `docs/ata9627-v3-calendar-event-test.md` 測試紀錄文件。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| Outlook Editor 權限驗證 | 已完成 | 100% | ATA-9627 已在 Outlook 端驗證 |
| 取得行事曆 (V2) 測試 | 已完成 | 100% | 僅回傳自身 Calendar |
| V3 Resource Calendar 事件讀取 | 待測 | 0% | 下一步測 `subject = TEST` |
| 三台車 Calendar ID 確認 | 進行中 | 33% | 先測 ATA-9627 |
| SharePoint 同步正式串接 | 待續 | 0% | 等 V3 讀取成功後進行 |

### 下一階段工作

1. 在 Power Automate 建立 `公務車功能測試-ATA9627事件讀取`。
2. 使用 `取得事件的行事曆檢視 (V3)`，Calendar Id 改填當時的候選 GUID `6049e1d1-b34c-4cca-b530-2c7c4b77abe9`。
3. 查詢 `utcNow()` 到 `addDays(utcNow(),7)`。
4. 確認是否可讀到主旨 `TEST`。
5. 回填 Calendar Id 實際設定方式、錯誤訊息或成功輸出。
6. 成功後再套用 Camry 與 Cross。

## v0.2.8 Master 更新 - ATA-9627 Calendar ID 已取得

更新日期：2026-07-15

本階段曾取得並視為 ATA-9627 Calendar ID 的候選 GUID：

`6049e1d1-b34c-4cca-b530-2c7c4b77abe9`

### 本次新增完成內容

- 將 ATA-9627 Calendar ID 記錄至 README、Power Automate 設計、測試紀錄與 Master 紀錄。
- 將 V3 事件讀取測試值由資源信箱 Email 改為候選 GUID；其有效性已於 v0.2.10 被實測推翻。
- 明確保留 V3 讀取狀態為待執行，不提前標記為通過。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| ATA-9627 候選 GUID 紀錄 | 已完成 | 100% | v0.2.10 已證實不適用 V3 |
| ATA-9627 V3 事件讀取 | 待執行 | 0% | 需實際執行 Flow |
| Camry / Cross Calendar ID 確認 | 待開始 | 0% | ATA-9627 成功後再處理 |
| SharePoint 同步正式串接 | 待續 | 0% | 等三台車讀取成功後進行 |

### 下一階段工作

1. 在 Power Automate 開啟 `公務車功能測試-ATA9627事件讀取`。
2. 將 `取得事件的行事曆檢視 (V3)` 的 Calendar Id 設為 `6049e1d1-b34c-4cca-b530-2c7c4b77abe9`。
3. 查詢期間使用 `utcNow()` 至 `addDays(utcNow(),7)`。
4. 執行 Flow，確認是否可取得主旨、起始時間、結束時間、是否全天、建立者／借用人、Event ID 與 iCalUId。
5. 若成功，回填測試結果並進入 Camry、Cross Calendar ID 取得與測試。

## v0.2.9 Master 更新 - ATA-9627 V3 測試實作版

更新日期：2026-07-15

本階段已將 ATA-9627 V3 事件讀取測試整理成 Power Automate 可落地執行版本，文件位置：

`power-automate/ata9627-v3-test-implementation.md`

### 本次新增完成內容

- 建立 `公務車功能測試-ATA9627事件讀取` 的實作步驟。
- 指定 V3 動作 Calendar Id 使用 `6049e1d1-b34c-4cca-b530-2c7c4b77abe9`。
- 新增事件數量、TEST 篩選、第一筆原始事件、欄位摘要等測試輸出設計。
- 明確定義 V3 成功前不得進入正式 SharePoint 同步流程。
- 建立成功後下一步 `公務車行事曆同步至 SharePoint - ATA9627 MVP` 草案。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| ATA-9627 候選 GUID 紀錄 | 已完成 | 100% | v0.2.10 已證實不適用 V3 |
| ATA-9627 V3 測試實作版 | 已完成 | 100% | 已建立可照 Power Automate 畫面操作的步驟 |
| ATA-9627 V3 實際執行 | 已執行／未通過 | 100% | `ErrorInvalidIdMalformed` |
| SharePoint 同步 MVP | 待開始 | 0% | 等 V3 事件讀取成功後才能建立 |

### 下一階段工作

1. 在 Power Automate 依 `power-automate/ata9627-v3-test-implementation.md` 建立或調整測試流程。
2. 執行 `公務車功能測試-ATA9627事件讀取`。
3. 回填全部事件數量、TEST 事件數量、第一筆事件原始資料。
4. 確認是否取得 subject、start、end、isAllDay、id、iCalUId。
5. 若成功，建立 `公務車行事曆同步至 SharePoint - ATA9627 MVP`。

## v0.2.10 Master 更新 - ATA-9627 候選 GUID V3 實測未通過

更新日期：2026-07-18

### 實測證據

| 項目 | 值 |
|---|---|
| Flow | `公務車功能測試-ATA9627事件讀取` |
| Flow ID | `7d66adc8-eccd-4cc0-9ab1-a031a6676df9` |
| Run ID | `08584172227294871773330429620CU04` |
| Calendar Id 輸入 | `6049e1d1-b34c-4cca-b530-2c7c4b77abe9` |
| 查詢期間 | `2026-07-14T00:00:00Z` 至 `2026-07-21T23:59:59Z` |
| 結果 | `400 BadRequest` |
| 錯誤 | `ErrorInvalidIdMalformed` / `The Id is invalid.` |
| clientRequestId | `6edfd333-e4b0-4d2d-a3e7-46ced3ee4270` |
| serviceRequestId | `8db0eba7-f781-4dd8-aacc-d7e4d81d3c70` |

### Master 判定

- `6049e1d1-b34c-4cca-b530-2c7c4b77abe9` 為候選物件 GUID，不是已驗證可供 Office 365 Outlook V3 使用的 Calendar ID。
- CAL-V3-06 未通過；CAL-V3-07、CAL-V3-08 因事件讀取未成立而阻擋。
- 本次不是 Access Denied，且 Calendar Id 已由執行輸入證實確實送達連接器。
- `公務車功能測試-ATA9627事件讀取` 仍顯示「開啟」且為每分鐘排程，需優先人工停用。
- M5 維持進行中／受阻；不得建立正式 SharePoint 同步 MVP。

## v0.2.11 Master 更新 - Resource Calendar 讀取阻擋解除

更新日期：2026-07-23

### 本次新增完成內容

- IT 已將 `ad.general@alp.global` 對三台公務車 Resource Mailbox 的權限提升為 Mailbox Full Access。
- 重測 `取得行事曆 (V2)` 與 `取得事件的行事曆檢視 (V3)`：確認兩者皆為連接器動作限制，僅能存取連線帳號自己的行事曆，與權限層級無關。
- 確認 `公務車功能測試-ATA9627事件讀取` 已由「開啟／每分鐘排程」變更為「關閉」。
- 驗證 `取得電子郵件 (V3)` 的 `原始信箱地址` 參數可指定 Resource Mailbox 並成功讀信，證實 Full Access 對郵件類動作有效。
- 找到正式解法：Office 365 Outlook 標準連接器（非 Premium）的「傳送 HTTP 要求」動作可直連 Graph，白名單物件包含 `calendar`、`events`、`calendars`。
- 以 `GET /v1.0/users/room_nhb4_car@alp.global/calendar/events?$filter=...` 實測成功（`statusCode 200`），取得 ATA-9627 真實借用紀錄，欄位完整（主旨、起訖時間、是否全天、借用人、iCalUId、最後修改時間、地點、重複規則）。
- 新增測試紀錄：`docs/ata9627-v3-calendar-event-test.md` 第十四節；新增 `release-notes/v0.2.11.md`。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| ATA-9627 Resource Calendar 讀取 | 已完成 | 100% | HTTP 要求 + `calendar/events` + `$filter` 驗證成功 |
| Camry / Cross Resource Calendar 讀取 | 待驗證 | 0% | 沿用 ATA-9627 相同路徑，待逐台實測 |
| 正式行事曆同步流程 | 待開始 | 0% | 讀取路徑已確認，可開始建置 |
| SharePoint 同步 MVP | 待開始 | 0% | 等三台車讀取驗證完成後同步進行 |

### Master 判定

- M5 Resource Calendar 事件讀取正式解除阻擋，判定為**通過**。
- 不需要 Exchange Administrator、Power Automate Premium 授權或額外 Azure AD App。
- 可以正式進入 `公務車行事曆同步至 SharePoint` 流程建置階段。

### 下一階段工作

1. 對 Camry（`room_nhb4_car_camry@alp.global`）、Cross（`room_nhb4_car_cross@alp.global`）重複相同的 HTTP 要求驗證。
2. 建立正式流程 `公務車行事曆同步至 SharePoint`，三台車共用同一組讀取、解析、寫入邏輯。
3. 依 `預約唯一鍵`（資源信箱 + 行事曆事件 ID）判斷新增或更新 SharePoint 項目。
4. 計算 `是否整天`、`預計通知時間`，並將 UTC 時間轉換為 `Asia/Taipei`。
5. 完成後建立下一個階段版本記錄，並依 `docs/version-control-workflow.md` 執行 commit、push、tag 與 GitHub Release。

## v0.2.12 Master 更新 - 正式行事曆同步流程建立並啟用排程

更新日期：2026-07-23

### 本次新增完成內容

- 完成三台公務車共用的正式流程 `公務車行事曆同步至SharePoint`（Flow ID `b6d1ec5c-0fc5-46c1-85b0-d8d68c72c0ce`）：Recurrence（15 分鐘）→ 車輛清單 → 套用至各項（三台車）→ 傳送 HTTP 要求 → 剖析 JSON → 套用至各項 1（逐筆事件）→ 計算預約唯一鍵／預計通知時間 → SharePoint 取得多個項目 → 條件 → 更新項目／建立項目。
- 依 `sharepoint/list-schema.md` 於「更新項目」「建立項目」兩分支完整寫入約 40 個欄位，含系統防呆欄位預設值。
- 排除設計問題：確認 Power Automate 動作／迴圈顯示名稱含空格時，從外部以名稱參照會被轉換為底線（例如 `套用至各項 1` 需寫成 `items('套用至各項_1')`），修正「更新項目」約 13 個受影響欄位。
- 流程儲存驗證零錯誤，流程檢查程式錯誤 0、警告 0。
- 已將流程由「關閉」切換為「開啟」，正式進入每 15 分鐘排程運作。
- 新增 `release-notes/v0.2.12.md`，更新 `docs/todo.md`、`power-automate/README.md`、`CHANGELOG.md`。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| ATA-9627 / Camry / Cross Resource Calendar 讀取 | 已完成 | 100% | 三台車皆採用相同 HTTP 直連 Graph 路徑 |
| 正式行事曆同步流程建立 | 已完成 | 100% | 已儲存零錯誤並開啟排程 |
| 正式行事曆同步流程端到端測試 | 待開始 | 0% | 尚未以實際 Outlook 預約驗證同步結果 |
| Teams 通知與回覆流程 | 待開始 | 0% | 等同步流程端到端測試通過後進行 |

### Master 判定

- M6「建立正式 `公務車行事曆同步至 SharePoint` 流程」正式完成，判定為**通過**。
- 流程已上線排程（每 15 分鐘執行一次），惟尚未完成端到端測試，`是否測試資料` 暫維持 `是` 作為安全預設值。
- 尚未建立 Concurrency Control，暫存測試流程 `公務車功能測試-郵件動作參數檢查(可刪除)` 尚未刪除，皆列為下一階段工作。

### 下一階段工作

1. 執行端到端測試：實際於 Outlook 建立／修改／取消預約，確認流程可在 15 分鐘內正確同步至 SharePoint。
2. 依 `docs/todo.md` P3 測試各項情境（整天借用、早上過早借用、取消、時間異動、重複觸發等）。
3. 端到端測試通過後，將 `是否測試資料` 切換為正式運作邏輯，並刪除暫存測試流程。
4. 建立 Concurrency Control，降低重複同步風險。
5. 進入 P2 階段，建立「公務車借用前 Teams 通知與回覆」流程。

## v0.2.13 Master 更新 - 執行期錯誤修正並完成首次成功執行驗證

更新日期：2026-07-24

### 本次新增完成內容

- v0.2.12 儲存驗證雖為零錯誤，但開啟排程後所有執行紀錄皆顯示失敗；本版本逐一排查並修正四種造成執行期失敗的錯誤：
  1. 兩處 Apply to each 迴圈的 `foreach` 參數誤加大括號（`@{items(...)}`），修正為 `@items(...)`。
  2. 「取得多個項目」篩選查詢誤用中文欄位顯示名稱，改以 SharePoint REST API 驗證後的正確內部名稱 `OData__x9810__x7d04__x552f__x4e00__x93`。
  3. 「條件」判斷式 `greater` 比較誤加大括號，修正為 `@length(...)`。
  4. 「建立項目」「更新項目」的「是否整天」欄位誤寫入中文字串，改為原生布林參照 `@items('套用至各項_1')?['isAllDay']`。
- 修正後手動觸發測試，運行紀錄首次出現「測試成功」；逐層展開執行追蹤確認三台車、內層事件迴圈、SharePoint 取得/建立/更新項目皆成功執行。
- 以 SharePoint REST API 直接查詢清單，確認三筆借用紀錄正確寫入，「是否整天」欄位為真正布林值（非文字字串）；後續排程再次執行後項目數未增加，確認「預約唯一鍵」防重複比對邏輯正常運作。
- 新增 `release-notes/v0.2.13.md`，更新 `docs/todo.md`、`power-automate/README.md`、`CHANGELOG.md`。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| 正式行事曆同步流程執行期錯誤修正 | 已完成 | 100% | 四種錯誤皆已修正並驗證 |
| 首次成功執行驗證 | 已完成 | 100% | 運行紀錄「測試成功」，SharePoint 資料驗證通過 |
| P3 各項情境測試 | 待開始 | 0% | 取消、時間異動、整天借用、重複觸發等尚待測試 |
| Teams 通知與回覆流程 | 待開始 | 0% | 等 P3 測試通過後進行 |

### Master 判定

- 正式流程 `公務車行事曆同步至SharePoint` 完成首次真正端到端成功執行，並以 SharePoint 實際資料驗證寫入正確性，判定為**通過**。
- v0.2.12「流程儲存驗證零錯誤」之敘述，就實際執行結果而言並不完整；本版本已補充說明並修正執行期錯誤，後續版本紀錄應留意「儲存零錯誤」不等於「執行成功」。
- 尚未涵蓋取消、時間異動等情境，`是否測試資料` 暫維持 `是`，Concurrency Control 與暫存測試流程刪除仍列為下一階段工作。

### 下一階段工作

1. 執行 P3 各項情境測試（取消、時間異動、整天借用、重複觸發等）。
2. 確認「我的流程」清單中是否有本流程的重複項目需清理。
3. 通過後將 `是否測試資料` 切換為正式運作邏輯，並刪除暫存測試流程。
4. 建立 Concurrency Control，降低重複同步風險。
5. 進入 P2 階段，建立「公務車借用前 Teams 通知與回覆」流程。

## v0.2.14 Master 更新 - 修正「更新項目」時間欄位 +8 小時位移嚴重錯誤

更新日期：2026-07-24

### 本次新增完成內容

- 執行 P3 情境測試時，透過測試紀錄連續三次 SharePoint REST API 比對，發現「更新項目」（Update，PATCH/MERGE）動作寫入借用起始時間、借用結束時間、預計通知時間、事件最後修改時間時，比「建立項目」（Create，POST）多了 8 小時（Taipei 時區 UTC 位移量），即使兩者運算式與來源資料完全相同。
- 以剖析 JSON 原始輸出比對前後兩次執行，確認 Graph 來源資料未變，排除運算式撰寫錯誤或來源資料變動；鎖定成因為 SharePoint 連接器 Create／Update 對「不含時區後綴的模糊時間字串」解讀邏輯不一致。
- 修正「更新項目」動作：借用起始/結束時間、最後同步時間、事件最後修改時間，移除不必要的 `convertTimeZone` 轉換，改為直接輸出 UTC 原始時間並明確加上 `Z` 時區後綴；預計通知時間因參照與「建立項目」共用的 Compose 輸出，改為在引用處以 `addHours(...,-8)` 反推回 UTC 再補 `Z` 後綴，避免影響「建立項目」既有正確行為。
- 儲存流程確認零錯誤，手動觸發測試（測試紀錄已存在，故走「更新項目」分支），以 SharePoint REST API 驗證借用起始/結束時間、預計通知時間皆恢復與「建立項目」當下一致的正確值。
- 新增 `release-notes/v0.2.14.md`，更新 `docs/todo.md`、`CHANGELOG.md`、`power-automate/README.md`。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| 「更新項目」時間欄位 +8 小時位移錯誤修正 | 已完成 | 100% | 已鎖定成因並以測試紀錄驗證通過 |
| 正式紀錄（Id=1、Id=2）時間欄位回復抽查 | 待執行 | 0% | 需等下次排程自動更新後抽查 |
| P3 其餘情境測試 | 待開始 | 0% | 取消、時間異動、整天借用、重複觸發等尚待測試 |
| Teams 通知與回覆流程 | 待開始 | 0% | 等 P3 測試通過後進行 |

### Master 判定

- 「更新項目」動作時間欄位 +8 小時位移錯誤，判定為**已修正並驗證通過**（以測試紀錄即時驗證）。
- 本版本修正並補充 v0.2.13「判定：通過」結論之不足——該結論僅驗證 Create 路徑，未涵蓋排程自動觸發的 Update 路徑；此為本專案文件慣例中「儲存零錯誤不等於執行正確」原則的再一次體現，這次更進一步顯示「單次執行正確不等於所有路徑（Create／Update）皆正確」。
- 正式（非測試）紀錄 Id=1、Id=2 過去因走過 Update 路徑可能已被寫入錯誤時間，待其下次排程自動更新後應會被本次修正覆寫回正確值，建議另行抽查確認。

### 下一階段工作

1. 抽查正式紀錄 Id=1、Id=2 於下次排程執行後，時間欄位是否已回復正確。
2. 繼續執行 P3 其餘情境測試（取消、時間異動、整天借用、重複觸發等）。
3. 確認「我的流程」清單中是否有本流程的重複項目需清理。
4. 通過後將 `是否測試資料` 切換為正式運作邏輯，並刪除暫存測試流程、建立 Concurrency Control。
5. 進入 P2 階段，建立「公務車借用前 Teams 通知與回覆」流程。

## v0.2.14 追加更新 - 正式紀錄抽查與 P3 情境測試結果

更新日期：2026-07-27

### 本次新增完成內容

- 以 SharePoint REST API 抽查正式紀錄 Id=1、Id=2，確認於下次排程自動觸發後，兩筆紀錄的借用起始/結束時間、預計通知時間皆已回復正確（與修正前相差精準 8 小時），且 `Modified` 時間皆晚於 v0.2.14 修正部署時間，證實修正對既有正式資料同樣有效。
- 執行 P3 情境測試：
  - **時間異動（FT-102／SG-T05 部分）**：通過。將測試事件時間由 03:00-04:00 改為 10:00-11:00，SharePoint 正確更新起訖時間與預計通知時間。
  - **整天借用（SG-T02／SG-T03）**：通過。以 Id=1 抽查證據確認起訖時間與預計通知時間皆為當天 08:00，符合整天借用邏輯，且不會產生前一天的錯誤通知時間。
  - **重複觸發（SG-T07）**：通過。以歷史執行記錄佐證，同一批事件經排程每 15 分鐘重複觸發數十次，SharePoint 對應紀錄皆維持單筆，Event ID/iCalUId 比對機制有效防止重複建立。
  - **取消預約（FT-103／SG-T04）**：**未通過，發現新問題**。刪除 Outlook 事件後，流程的「取得多個項目」不再回傳該事件，目前無任何機制偵測事件消失並將 SharePoint 紀錄標記為已取消；SharePoint 對應紀錄會維持在刪除前的最後狀態，`已取消` 仍為否，後續通知不會停止。
- 診斷發現一項操作性事實（非流程錯誤）：流程的行事曆讀取為未來/當下時間範圍導向，過去日期的事件不會被讀取到；測試時若事件日期已成為過去，即使流程執行成功，也不會反映任何異動，需將測試事件移至未來日期方能正確驗證。
- 已更新 `test-cases/function-test-plan.md`（FT-101～FT-103、SG-T01～SG-T07 狀態列）、`docs/todo.md`（v0.2.14 待辦事項第 6～8 項）。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| 正式紀錄（Id=1、Id=2）時間欄位回復抽查 | 已完成 | 100% | 兩筆紀錄時間欄位皆已確認回復正確 |
| P3 情境測試 - 時間異動 | 已完成 | 100% | 通過 |
| P3 情境測試 - 整天借用 | 已完成 | 100% | 通過（以 Id=1 抽查證據佐證） |
| P3 情境測試 - 重複觸發 | 已完成 | 100% | 通過（以歷史執行記錄佐證） |
| P3 情境測試 - 取消預約 | 已完成（結果：未通過） | 100% | 發現新問題：事件刪除無法反映至 SharePoint，待補強偵測邏輯 |
| 事件取消偵測補強邏輯 | 待開始 | 0% | 新增待辦，需比對 SharePoint 既有紀錄與行事曆讀取結果，標記消失的 Event ID 為已取消 |
| Teams 通知與回覆流程 | 待開始 | 0% | 等取消偵測補強完成、`是否測試資料` 切換後進行 |

### Master 判定

- 正式紀錄 Id=1、Id=2 時間欄位回復，判定為**已驗證通過**。
- P3 情境測試中，時間異動、整天借用、重複觸發三項，判定為**通過**。
- 取消預約情境，判定為**未通過**：這是本專案迄今發現的第一個非時區類、屬於「架構缺口」的問題——流程設計上從未包含「偵測資料消失」的比對步驟，需在下一開發階段新增此邏輯，而非單純修正運算式即可解決。
- 測試紀錄 Id=4（其 Outlook 來源事件已刪除、SharePoint 紀錄未被標記取消）予以保留，作為此問題的可重現證據，暫不清理。

### 下一階段工作

1. 設計並實作事件取消偵測補強邏輯（比對 SharePoint 既有紀錄與本次行事曆讀取結果，標記消失的 Event ID 為已取消，停止後續通知）。
2. 待車輛、主旨、借用人異動與舊卡片失效機制（SG-T05 其餘部分、SG-T06）於 Teams Adaptive Card 流程開發後一併測試。
3. 確認「我的流程」清單中是否有本流程的重複項目需清理。
4. 全部通過後將 `是否測試資料` 切換為正式運作邏輯，並刪除暫存測試流程、建立 Concurrency Control。
5. 進入 P2 階段，建立「公務車借用前 Teams 通知與回覆」流程。

## v0.2.15 Master 更新 - 事件取消偵測補強邏輯完成並驗證通過

更新日期：2026-07-27

### 本次新增完成內容

- 設計並實作「事件取消偵測」補強邏輯：於既有流程的逐台車迴圈中，新增陣列變數 `CurrentEventKeys` 記錄本次執行實際讀取到的所有事件唯一鍵；新增 SharePoint 取得多個項目動作，篩選該車輛「未取消」且「借用起始時間」落在本次讀取時間窗內的既有紀錄；新增巢狀迴圈與條件判斷，找出唯一鍵未出現在本次讀取結果中的既有紀錄，標記為 `已取消 = 是`、`事件同步狀態 = 已取消`、`取消時間 = utcNow()`。
- 儲存流程確認零錯誤（「您的流程已準備就緒」）。
- 手動觸發測試並以 SharePoint REST API 驗證：
  - 既定測試案例 Id=4（其 Outlook 事件已於先前測試階段被刪除）：正確標記為已取消。
  - 額外發現並正確標記一筆先前測試階段遺留、未被追蹤到的孤兒紀錄 Id=1（Michael Chiu）：經 Graph API 查詢結果為空、Outlook 網頁行事曆雙重確認其來源事件確實已刪除，證實為正確判定而非誤判。
  - 仍然有效的未來借用 Id=2（Andy Yang）、Id=3（Leo Li）：未受影響，維持 `已取消 = 否`。
- 已更新 `test-cases/function-test-plan.md`（FT-103、SG-T04 狀態改為通過）、`docs/todo.md`（新增 v0.2.15 待辦事項）、`CHANGELOG.md`。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| 事件取消偵測補強邏輯 | 已完成 | 100% | 已實作並驗證通過 |
| P3 情境測試 - 取消預約 | 已完成 | 100% | 通過（FT-103／SG-T04） |
| P3 情境測試整體 | 已完成 | 100% | 時間異動、整天借用、重複觸發、取消預約全數通過 |
| Teams 通知與回覆流程 | 待開始 | 0% | 等 `是否測試資料` 切換、重複項目清理後進行 |

### Master 判定

- 事件取消偵測補強邏輯，判定為**已實作並驗證通過**。
- P3 情境測試（時間異動、整天借用、重複觸發、取消預約）全數判定為**通過**，`test-cases/function-test-plan.md` 對應狀態列已更新。
- 本次驗證意外發現並正確處理一筆先前測試階段遺留、未被追蹤到的孤兒紀錄（Id=1），提升對本補強邏輯正確性的信心，證實其具備偵測任意時間點資料飄移的能力，不僅限於刻意建立的測試案例。
- 已知限制：SharePoint 篩選查詢的日期比較字串未明確加註 UTC 標記，與 Graph API 端解讀基準有落差，惟因篩選窗口採「寧可多檢查」設計，不影響本次驗證結果，留待未來版本一併統一格式。

### 下一階段工作

1. 確認「我的流程」清單中是否有本流程的重複項目需清理。
2. 將 `是否測試資料` 切換為正式運作邏輯，並刪除暫存測試流程 `公務車功能測試-郵件動作參數檢查(可刪除)`。
3. 建立 Concurrency Control（Degree of Parallelism），降低重複同步風險。
4. 進入 P2 階段，建立「公務車借用前 Teams 通知與回覆」流程。

## v0.2.16 Master 更新 - 流程清理、正式化切換與 Concurrency Control 完成

更新日期：2026-07-27

### 本次新增完成內容

- 檢視「我的流程」清單，發現一個同名重複流程（Flow ID `a3357db1-a6f0-47ad-bc8f-e182756e1263`，建立於 2026-07-23），狀態為開啟、每 15 分鐘執行一次；展開執行紀錄確認其動作結構僅為早期原型（讀取與剖析，無 SharePoint 寫入），排除資料衝突疑慮後將其關閉。
- 額外發現兩個文件記載「已關閉」但實際仍在背景執行的測試流程：`公務車功能測試-資源信箱讀取`（每約 1 分鐘執行）、`公務車功能測試-SharePoint清單連線`（每約 1 分鐘執行），已一併關閉。
- 刪除暫存測試流程 `公務車功能測試-郵件動作參數檢查(可刪除)`（手動觸發類型，與正式流程無關聯）。
- 透過「程式碼檢視」定位到正式流程「更新項目」「建立項目」兩處皆將 `是否測試資料` 硬編碼為 `是`，已分別改為 `否`，切換為正式運作邏輯；儲存流程確認零錯誤。
- 在 Recurrence 觸發程序啟用「並行控制」，將平行處理原則的程度設為 `1`，避免流程重疊執行造成 SharePoint 重複或競爭寫入；同時確認三個 Apply to each 迴圈（套用至各項、套用至各項 1、套用至各項 2）預設即為循序執行，無需額外調整。
- 已更新 `release-notes/v0.2.16.md`、`CHANGELOG.md`、`docs/todo.md`。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---|---|
| 「我的流程」清單清理 | 已完成 | 100% | 重複流程與背景執行測試流程皆已關閉，暫存測試流程已刪除 |
| `是否測試資料` 正式化切換 | 已完成 | 100% | 未來紀錄適用；既有紀錄不受影響，屬已知限制 |
| Concurrency Control | 已完成 | 100% | 觸發層級限制為 1；Apply to each 迴圈確認維持循序執行 |
| Teams 通知與回覆流程（P2） | 待開始 | 0% | 下一階段工作 |

### Master 判定

- v0.2.15 遺留的三項待辦事項（「我的流程」清理、`是否測試資料` 正式化、Concurrency Control）皆判定為**已完成**。
- 已知限制：既有測試紀錄（Id=1～4）的 `是否測試資料` 欄位未被追溯更新，是否批次修正留待下一步決定；SharePoint 篩選查詢日期字串未加註 UTC 標記的已知限制（v0.2.15 記錄）仍存在，不影響正確性判定。

### 下一階段工作

1. 視需要決定既有測試紀錄的 `是否測試資料` 是否批次修正。
2. 進入 P2 階段，建立「公務車借用前 Teams 通知與回覆」流程。


## v0.3.0 Master 更新 - P2 公務車借用前 Teams 通知與回覆流程完成並驗證通過

更新日期：2026-07-29

### 本次新增完成內容

・ 建立正式流程 `公務車借用前Teams通知與回覆`（Flow ID `8187f981-d4a8-4ca5-89a7-684f69c278f5`）：Recurrence（5 分鐘）→ 取得多個項目（8 個條件判斷）→ Apply to each → 張貼調適型卡片並等候回覆 → 剖析 JSON → 取得項目（重新讀取最新記錄）→ 條件1（SG-T06 舊卡片防呆，8 個判斷式）→ True：更新項目／False：張貼固定提示訊息。
・ 修正「取得項目」action 的 `id` 參數在特定情境下傳入 null 導致執行失敗的問題。
・ 實作 SG-T06 舊卡片防呆邏輯，過程中發現並修正兩類 Bug：
  ・ 條件式運算式缺少 `@` 前綴、括號存取子誤寫為單一字串鍵值（例如 `?['body/OData_xxx/Value']`）而非鏈式括號存取（`?['body']?['OData_xxx']?['Value']`）。
    ・ `cardVersion` 型別不一致：Adaptive Card 回覆資料中 `cardVersion` 為字串型別，SharePoint 對應欄位為數字型別，`equals()` 比較因型別不同恆為 `false`；改以 `string()` 包裝兩側比較值後解決。此 Bug 曾在語法修正完成後仍導致 SG-T06 條件1 誤判為 false，是本次除錯耗時最長的一項，透過逐欄比對 `剖析 JSON` 與 `取得項目` 原始輸出定位根因。
    ・ 手動觸發流程並確認 FT-108：Teams 卡片正確送出，內容含車輛、借用日期、時間、使用事由與規範提示。
    ・ 借用人於 Teams 回覆卡片，確認 FT-109～FT-111：填寫共乘人數並勾選規範後送出，SharePoint 正確更新 `領鑰狀態 = 已完成確認`、`共乘人數`、`規範確認狀態`、`Teams 回覆時間`；Adaptive Card 之共乘人數與規範確認皆為必填/必勾選，未完整填寫時卡片端即擋下無法送出。
    ・ 完成 SG-T06 正向與負向路徑測試：
      ・ 正向：卡片資料與目前 SharePoint 記錄一致，條件1 正確判定為 `true`，正確更新 SharePoint。
        ・ 負向：卡片送出前，將測試記錄的「行事曆事件 ID」異動，模擬卡片已過期；條件1 正確判定為 `false`，Teams 正確顯示「此借用已取消或已變更，請重新確認最新借用資訊。」，且以 SharePoint REST API 直接查詢確認 `領鑰狀態` 等欄位維持異動前狀態，`更新項目` 動作確實未執行。
        ・ 測試過程中發現一項操作疏失並記錄為除錯經驗：重設測試記錄以進行 SG-T06 負向測試時，僅重設了「領鑰狀態」而遺漏「通知狀態」，導致主要「條件」判斷式仍為 `false`（因該欄位需為「未通知」才會觸發送卡），流程因此未送出新卡片；補齊兩欄位重設後才成功觸發。此問題也曾一度導致 Recurrence 每 5 分鐘重複對同一筆測試記錄送出新卡片給借用人（因記錄一直維持在符合觸發條件的狀態），已透過設定測試記錄「領鑰狀態 = 取消」、「事件同步狀態 = 已取消」，並取消所有仍在等待卡片回覆的重複執行個體加以排除。

        ### 目前狀態

        | 功能 | 狀態 | 完成度 | 備註 |
        |---|---|---|---|
        | 公務車借用前 Teams 通知與回覆流程建置 | 已完成 | 100% | Flow ID `8187f981-d4a8-4ca5-89a7-684f69c278f5` |
        | FT-108（Teams Adaptive Card 發送） | 已完成 | 100% | 通過 |
        | FT-109～FT-111（借用人回覆寫回 SharePoint） | 已完成 | 100% | 通過 |
        | SG-T06（舊卡片防呆，正向＋負向路徑） | 已完成 | 100% | 通過，詳見 `release-notes/v0.3.0.md` |
        | 通知發送時間欄位實際寫入 | 待開始 | 0% | 目前以「通知狀態」欄位反映是否已通知，尚未補上時間戳記寫入邏輯 |
        | FT-104～FT-107、FT-112、FT-113、SG-T08、SG-T09 | 待開始 | 0% | 尚待執行測試案例 |
        | P4 上線文件與操作手冊 | 待開始 | 0% | 下一階段工作 |

        ### Master 判定

        ・ P2 階段「公務車借用前 Teams 通知與回覆」流程，判定為**建置完成並驗證通過**：FT-108～FT-111 全數通過，SG-T06 正向與負向路徑皆驗證通過。
        ・ 本次除錯過程確認並記錄一項可能影響其他流程的一般性風險：測試/除錯時若只重設部分「應同時代表同一狀態」的欄位（本例為領鑰狀態與通知狀態），可能導致主要條件判斷式邏輯不一致而產生非預期行為（包括重複發送通知），後續測試應建立「重設清單」逐一核對，而非僅重設單一欄位。
        ・ 已知限制：「通知發送時間」欄位尚未有實際寫入邏輯；FT-104～FT-107（通知時間規則）、FT-112（承辦人後台領鑰判斷）、FT-113（同車重複預約）、SG-T08（Event ID 唯一鍵）、SG-T09（重複資料異常）等測試案例尚待執行。

        ### 下一階段工作

        ・ 補上「通知發送時間」欄位的實際寫入邏輯。
        ・ 執行 FT-104～FT-107、FT-112、FT-113、SG-T08、SG-T09 等尚待執行測試案例。
        ・ 進入 P4 階段：建立行政/總務操作手冊、異常處理說明、日常維護說明，完成上線前驗收紀錄。


## v0.3.1 Master 更新 - P2 收尾測試補齊完成

更新日期：2026-07-30

### 本次新增完成內容

・ 依既有測試計畫，執行並補齊 FT-104～FT-107、FT-112、FT-113、SG-T08、SG-T09 共 8 項待測案例。
・ FT-104（借用 09:30）／FT-105（08:30）／FT-106（早於 08:00，實測 07:30）／FT-107（整天借用）：於 ATA-9627 行事曆分別建立對應測試事件，經 `公務車行事曆同步至SharePoint` 流程同步後，SharePoint 預計通知時間之計算結果與「借用前 1 小時、最早 08:00、整天一律 08:00」規則完全相符，4 項全數通過；測試完成後已清除對應之 Outlook 測試事件與 SharePoint 測試紀錄。
・ FT-112：以 SharePoint REST API 查詢確認 `領鑰狀態` 欄位於所有正式紀錄皆正確填寫且可篩選，承辦人可據此判斷是否發放鑰匙，判定通過。
・ SG-T08：以既有正式紀錄佐證（兩筆商業發展處 ALP BD用車 紀錄主旨相同、預約唯一鍵／Event ID 不同，SharePoint 正確區分為獨立兩筆），判定通過。
・ FT-113：測試並複核目前流程架構後發現，借用事件是以 Editor 委派權限直接建立於資源信箱行事曆，並非透過會議邀請送交資源信箱，因此不會觸發 Outlook 資源信箱原生的雙重預約阻擋機制；現階段系統未在任何層級強制避免同車重複預約，判定為未通過並列為已知限制，待後續評估補強方案（例如流程內新增同車時段重疊檢查邏輯）。
・ SG-T09（重複資料異常標記）本輪測試循環尚未實際執行，維持待驗證狀態，列為後續待辦。
・ 已更新 `test-cases/function-test-plan.md`、`CHANGELOG.md`、`docs/todo.md` 反映最新測試結果。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---:|---|
| FT-104～FT-107（通知時間邊界） | 已完成 | 100% | 均通過 |
| FT-112（後台領鑰判斷） | 已完成 | 100% | 通過 |
| SG-T08（Event ID 唯一鍵） | 已完成 | 100% | 通過 |
| FT-113（同車重複預約） | 已完成（結果：未通過） | 100% | 發現現行流程未強制避免，列為已知限制 |
| SG-T09（重複資料異常） | 待執行 | 0% | 本輪循環尚未實際進行驗證 |
| P4 上線文件與操作手冊 | 待開始 | 0% | 下一階段工作 |

### Master 判定

・ P2 階段收尾測試（FT-104～FT-107、FT-112、FT-113、SG-T08、SG-T09）共 8 項已全數執行，其中 7 項判定為**已完成**（FT-104～FT-107、FT-112、SG-T08 通過；FT-113 已完成測試但結果為未通過），1 項（SG-T09）尚未實際執行。
・ FT-113 發現並非流程錯誤，而是架構層級的已知限制：目前借用事件以 Editor 委派權限直接建立於資源信箱行事曆，未透過會議邀請送交至資源信箱，因此無法觸發 Outlook 資源信箱原生的雙重預約阻擋機制，需於後續版本評估補強方案。
・ 本輪測試未實際執行 SG-T09（需人為建立重複預約唯一鍵資料作為模擬），已實事求是列為待驗證，不作虛假通過判定。

### 下一階段工作

1. 評估 FT-113 補強方案（例如於流程內新增同車時段重疊檢查邏輯，若發現重疊則拒絕建立或標記異常）。
2. 安排執行 SG-T09：人為建立相同 `預約唯一鍵` 的多筆資料作為模擬，驗證系統是否正確標記重複異常且不發送 Teams 通知。
3. 補上「通知發送時間」欄位實際寫入邏輯。
4. 進入 P4 階段：建立行政/總務操作手冊、異常處理說明、日常維護說明，完成上線前驗收紀錄。

## v0.3.2 Master 更新 - FT-113 補強邏輯完成並驗證通過

更新日期：2026-08-03

### 本次新增完成內容

・ 針對 v0.3.1 列為已知限制的 FT-113（同車重複預約），實作補強方案：於「套用至各項 1」逐筆事件迴圈中，新增第二個「取得多個項目」動作篩選同一台車、`已取消 = 否`、`預約唯一鍵` 不同的既有紀錄，再以「篩選陣列」比對借用起訖時間是否重疊；「建立項目」與「更新項目」皆依比對結果將 `重複資料檢查結果` 寫入 `異常` 或 `正常`，不阻擋建立、僅標記供承辦人後續判斷。
・ 初版以測試事件（TEST-FT113-A／B，2026-07-31）驗證，手動觸發後標記邏輯正常；但排程自動觸發一次後重新檢查，發現兩筆紀錄的標記皆被靜默重置為 `正常`，補強邏輯於首次執行後即失效。
・ 診斷根因：「建立項目」以 `convertTimeZone` 將 Graph 回傳的 UTC 時間轉為 Taipei 本地時間（無時區後綴）寫入借用起訖時間，「更新項目」則直接對原始 UTC 時間補 `Z` 後綴；SharePoint 讀出時一律視為 UTC 並補 `Z`，導致兩動作寫入的時間字串代表意義不一致，只要紀錄曾被排程觸發的「更新項目」路徑寫入過一次，「篩選陣列」的時間重疊比較就會失準。
・ 修正：統一「建立項目」「更新項目」的借用起始/結束時間運算式為直接輸出原始 UTC 並加 `Z` 後綴，同步簡化「篩選陣列」比對條件為兩側皆比較原始 UTC。流程儲存確認零錯誤。
・ 建立新測試事件對（TEST-FT113-A3／B3，2026-08-04 15:00-15:30 / 15:15-15:45，故意重疊）重新驗證；起初以「邀請出席者」方式建立時，發現資源信箱因衝突自動拒絕邀請、事件未進入資源行事曆，改以「開啟另一個信箱」直接於資源行事曆本身建立事件解決。
・ 手動觸發流程：兩筆重疊事件皆成功建立為新紀錄，正確呈現一筆「正常」、一筆「異常」的非對稱標記；15 分鐘後排程自動觸發一次（Update 路徑），兩筆紀錄標記重新評估後互換，但未再出現雙雙被靜默重置為「正常」的錯誤，證實修正有效。
・ FT-113 狀態由「未通過（已知限制）」更新為「通過」。
・ 新增管理員手動確認操作程序文件 `docs/admin-manual-confirmation-procedure.md`，明確規定承辦人手動確認借用（借用人未透過 Teams 卡片回覆時）必須同時更新「領鑰狀態」與「通知狀態」等欄位，避免重蹈 v0.3.0 除錯階段發現的欄位不同步問題。
・ 已更新 `test-cases/function-test-plan.md`（FT-113 狀態）、`CHANGELOG.md`、`docs/todo.md`。

### 目前狀態

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---:|---|
| FT-113 同車時段重疊偵測邏輯 | 已完成 | 100% | 不阻擋建立，標記 `重複資料檢查結果` |
| 時間格式不一致隱性 Bug 修正 | 已完成 | 100% | 已統一 Create/Update 兩路徑時間格式 |
| FT-113 重新驗證（Create→Update 轉換） | 已完成 | 100% | 標記可正確跨路徑維持，不會被靜默重置 |
| 管理員手動確認操作程序文件 | 已完成 | 100% | `docs/admin-manual-confirmation-procedure.md` |
| 測試資料清理 | 待執行 | 0% | Outlook 測試事件與 SharePoint 測試紀錄（ID 13、14、16、17）待清理，清單見 `release-notes/v0.3.2.md` |
| SG-T09（重複資料異常） | 待執行 | 0% | 沿用既有待辦，尚未實際執行 |

### Master 判定

・ FT-113（同車重複預約）補強邏輯，判定為**已實作並驗證通過**，狀態由「未通過（已知限制）」更新為「通過」。
・ 本次驗證過程額外發現一項隱性 Bug（Create/Update 兩路徑時間格式不一致導致防呆標記失效），已修正並以 Create→Update 完整轉換路徑重新驗證，避免重蹈先前僅驗證單一路徑即宣告通過的判斷失誤模式（同類教訓見 v0.2.14）。
・ 已知限制：SharePoint DispForm 顯示畫面偶爾出現時間顯示疑似疊加時區偏移的情形，重新整理後恢復正常，研判為前端顯示層快取/在地化行為，不影響底層資料與比對邏輯正確性，列為觀察項目。
・ 本輪測試產生的 Outlook 測試事件與 SharePoint 測試紀錄尚待清理，清單已記錄於 `release-notes/v0.3.2.md`。

### 下一階段工作

1. 清理本輪 FT-113 測試資料（Outlook 測試事件 TEST-FT113-A/B、A2/B2、A3/B3；SharePoint 測試紀錄 ID 13、14、16、17）。
2. 安排執行 SG-T09：人為建立相同 `預約唯一鍵` 的多筆資料作為模擬，驗證系統是否正確標記重複異常且不發送 Teams 通知。
3. 補上「通知發送時間」欄位實際寫入邏輯。
4. 進入 P4 階段：建立行政/總務操作手冊、異常處理說明、日常維護說明，完成上線前驗收紀錄。

## v0.3.3 Master 更新 - SG-T09 重複資料異常標記補強邏輯完成並驗證通過

更新日期：2026-08-04

### 本次新增完成內容

・ 於「公務車行事曆同步至SharePoint」流程新增 SG-T09 補強邏輯：條件2（比對 `取得多個項目` 回傳之同一 `預約唯一鍵` 既有紀錄筆數是否 >1）→ 套用至各項3（逐筆處理所有相符紀錄）→ 更新項目2（將 `重複資料檢查結果` 標記為 `疑似重複`）。
・ 實作過程中發現並修正三處運算式雙重跳脫 Bug，皆為同一根因——直接以鍵盤輸入或透過新版設計工具的「插入運算式」流程編輯既有欄位時，儲存後仍缺少正確的 `@` 前綴、或誤帶多餘一層 `@{}`，導致設計時流程檢查通過（零錯誤）但執行期發生 `InvalidTemplate`（`'greater' expects two parameter of matching types... invoked with values of type 'String' and 'Integer'`）：
  1. 條件2 比較左值 `length(...)` 缺少 `@` 前綴，被存成純文字字串而非運算式。
  2. 套用至各項3 的 `foreach` 來源誤存為 `@@{body(...)}`（雙重跳脫），修正為 `@body(...)`（單一 `@`，不加大括號，直接取得陣列本身而非字串化文字）。
  3. 更新項目2 的識別碼參數同樣誤存為 `@@{items(...)}`，修正為 `@items(...)`。
・ 修正方式：比對既有正常運作之「條件」動作（FT-113）的相同語法作為對照基準；於欄位為空時直接鍵入純函式文字（不含 `@`）可正常儲存但仍非運算式，需額外重新清空並改以帶 `@` 前綴的寫法重新輸入，儲存後以唯讀「程式碼檢視」比對確認雙引號內容與參照動作完全一致，才視為修正成功。
・ 修正過程中額外發現更新項目2 存在更嚴重的資料污染風險：除 `重複資料檢查結果` 外，還誤將 `已取消`、`已失效` 兩個防呆欄位強制寫入 `true`，並以固定字串覆蓋 `車輛名稱`、`規範確認狀態`、`領鑰狀態`、`是否測試資料`、`功能測試結果`、`是否整天` 等六個與重複標記完全無關的欄位——研判為由既有取消偵測用途的「更新項目」動作複製而來，複製後未清除多餘欄位設定所致。若未修正，正式上線後任何一筆重複紀錄都會被連帶誤標記為已取消、已失效，並覆蓋其車輛、領鑰狀態等真實資料。已於「進階參數」逐一移除六個無關欄位，修正後更新項目2 僅保留識別碼與 `重複資料檢查結果` 兩個參數。
・ 流程儲存確認零錯誤（「您的流程已準備就緒」），手動觸發測試：第一次執行（僅有 TEST-SGT09-A 一筆紀錄）成功，條件2 正確評估為假（筆數未大於 1），確認修正後的運算式於執行期不再拋出型別錯誤。
・ 以 SharePoint REST API 直接查詢既有測試紀錄 TEST-SGT09-A（ID 19）之完整 `預約唯一鍵` 值，並以相同鍵值透過 REST API 建立第二筆人為重複紀錄 TEST-SGT09-B（ID 21，人為重複測試標註於標題），避免手動於 SharePoint 表單複製貼上長字串時發生截斷或誤植風險。
・ 再次手動觸發流程，以 REST API 查詢兩筆紀錄最終狀態，確認 ID 19、ID 21 之 `重複資料檢查結果` 皆已正確更新為 `疑似重複`，SG-T09 核心判定邏輯驗證通過。
・ 驗證過程中發現 ID 21 的 `已取消`、`已失效` 欄位顯示為 `true`，經追查「條件」（原有 FT-113／SG-02 既有 upsert 邏輯）之「更新項目」動作程式碼確認：該動作以 `first(body('取得多個項目')?['value'])?['ID']` 僅更新第一筆相符紀錄（即 ID 19），且明確將 `已取消`、`已失效` 寫入 `false`，並無被本次修改影響；ID 21 之 `true` 值純粹是本次以 REST API 建立測試資料時未指定該兩個欄位、沿用 SharePoint 清單欄位預設值所致，非流程邏輯錯誤，僅為測試資料本身的既有狀態，不影響 SG-T09 判定結果。
・ 複核「公務車借用前Teams通知與回覆」流程既有 8 項條件式之程式碼，確認其中一項為 `equals(重複資料檢查結果, "正常")`（相等比對，非白名單排除法），代表 `疑似重複`、`異常`（FT-113 沿用同一欄位）或任何非 `正常` 之值皆會使該條件式為假、排除於發送 Teams 通知對象之外，確認符合 SG-T09「不發送 Teams 通知」要求，無需修改通知流程。
・ SG-T09 狀態由「待執行」更新為「通過」。

### 已知限制與待辦

| 功能 | 狀態 | 完成度 | 備註 |
|---|---|---:|---|
| SG-T09 條件2／套用至各項3／更新項目2 邏輯實作 | 已完成 | 100% | 三處運算式雙重跳脫 Bug 已修正 |
| 更新項目2 誤寫入欄位修正 | 已完成 | 100% | 已移除 已取消／已失效／車輛名稱 等六個無關欄位，僅保留 重複資料檢查結果 |
| SG-T09 重複標記驗證 | 已完成 | 100% | 以 REST API 建立人為重複紀錄驗證通過 |
| Teams 通知流程排除重複標記複核 | 已完成 | 100% | 確認 `等於正常` 相等比對，`疑似重複`／`異常` 皆會被排除 |
| 測試資料清理 | 待執行 | 0% | Outlook 測試事件 TEST-SGT09-A（2026/8/4 14:00-14:30，公務車9627-B4-永聯內湖辦公室資源行事曆）；SharePoint 測試紀錄 TEST-SGT09-A（ID 19）、TEST-SGT09-B（ID 21）待清理 |
| `重複資料檢查結果` 欄位語意重疊 | 觀察項目 | - | FT-113（異常）與 SG-T09（疑似重複）共用同一欄位，若同一紀錄同時符合兩種異常情境，後執行動作（本次為 SG-T09 條件2）之寫入值會覆蓋前者；因通知流程僅需判斷「是否等於正常」，不影響通知阻擋邏輯正確性，建議後續版本評估拆分為獨立欄位 |

### Master 判定

・ SG-T09（重複資料異常標記）判定為**已完成並驗證通過**：人為建立相同 `預約唯一鍵` 之重複紀錄後，系統正確標記兩筆紀錄為 `疑似重複`，且複核確認 Teams 通知流程既有邏輯已能正確排除此類紀錄，不發送通知。
・ 本次修正過程再次印證同類教訓（同見 v0.2.14、v0.3.2）：新增動作若由既有動作複製而來，務必逐一檢視每個欄位參數是否為當前情境所需，避免沿用不相關的舊有寫入邏輯造成資料污染；本次更新項目2 若未經逐一檢查，將於正式環境對真實重複紀錄造成已取消／已失效誤標記的嚴重後果。
・ 已知限制：`重複資料檢查結果` 欄位由 FT-113、SG-T09 共用但語意不同，已列為觀察項目，非阻擋性 Bug。

### 下一階段工作

1. 清理本輪 SG-T09 測試資料（Outlook 測試事件 TEST-SGT09-A；SharePoint 測試紀錄 ID 19、21）。
2. 評估是否將 `重複資料檢查結果` 拆分為獨立欄位，避免 FT-113 與 SG-T09 語意共用同一欄位互相覆蓋。
3. 補上「通知發送時間」欄位實際寫入邏輯。
4. 進入 P4 階段：建立行政/總務操作手冊、異常處理說明、日常維護說明，完成上線前驗收紀錄。

## v0.3.4 Master 更新 - P4 上線前文件補齊；發現並暫停重複發送通知 Bug（Bug#1）

更新日期：2026-08-06（補記；原變更完成於 2026-08-04，Bug 診斷與止血於 2026-08-06）

### 本次新增完成內容

・ 建立四份 P4 上線文件：`docs/operation-manual.md`（行政／總務操作手冊）、`docs/incident-handling-guide.md`（異常處理說明）、`docs/maintenance-guide.md`（日常維護說明）、`test-cases/go-live-acceptance-record.md`（上線前驗收紀錄）。
・ 完成 FT-113 測試資料清理（SharePoint ID 13/14/16/17、Outlook 全部測試事件），SG-T09 測試資料（TEST-SGT09-A、ID 19/21）留待專案負責人自行清理。
・ **2026-08-06 員工回報「重複收到多張借車通知卡片」且「填寫共乘人數送出後無回應」**，診斷確認為單一根因（非兩個獨立 Bug，詳見 `docs/incident-handling-guide.md` 第三節）：（1）『公務車借用前Teams通知與回覆』流程的 Recurrence 觸發程序自 v0.3.0 建立以來，並行控制一直維持關閉（系統預設允許並行執行個體），與同步流程於 v0.2.16 已建立的「並行控制=1」防護不一致；（2）『通知狀態』欄位僅在『張貼調適型卡片並等候回覆』（阻塞式、等候使用者回覆）完成並通過 SG-T06 防呆後才寫回『已通知』，等候期間仍為『未通知』，導致每 5 分鐘一次的排程重複啟動新執行個體、對同一位借用人反覆發送新卡片，直到對方回覆為止；「送出後無反應」為其下游連鎖症狀（其餘重複卡片於借用人回覆其中一份後，被 SG-T06 判定為過期，導向 False 分支僅回覆一則未反映真實原因的固定提示）。
・ 已立即將『公務車借用前Teams通知與回覆』（Flow ID `8187f981-d4a8-4ca5-89a7-684f69c278f5`）與『公務車行事曆同步至SharePoint』（Flow ID `b6d1ec5c-0fc5-46c1-85b0-d8d68c72c0ce`）兩流程全部關閉止血。
・ 修正方向（Bug#1）：發送 Adaptive Card 前即寫入「通知狀態＝已通知」作為送卡鎖定、為 Recurrence 觸發程序補上並行控制＝1、改寫 SG-T06 False 分支提示文字為明確說明重複卡片情境——已於本次（v0.3.4 期間）實作完成（對應 Task #106）。

### Master 判定

・ P4 四份上線文件判定為**已完成**。
・ 2026-08-06 回報之重複發送通知 Bug（Bug#1）診斷確認為系統性根因，修正邏輯已實作完成，但**兩流程在專案負責人明確同意前，維持關閉，不重新啟用**（測試階段治理原則）。

### 下一階段工作

1. 評估員工循環預借同一台公務車、每日重複收到通知卡片的獨立情境（非 Bug#1，屬正常借用行為下的體驗問題），設計「自動延用」邏輯（詳見下方 v0.3.5）。
2. SG-T09 測試資料清理完成後，會同 Bug#1 修正驗證結果，一併提交專案負責人審閱是否可重新開啟兩流程。

## v0.3.5 Master 更新 - 循環預借「自動延用」邏輯評估後不採用，回復逐日發送借用人確認卡片

更新日期：2026-08-06（初版實作）／2026-08-07（設計重新評估後移除，回復逐日發卡原設計）

### 背景

員工若連續多日借用同一台公務車（例如同一趟出差分多天預約、或固定排班用車），『公務車行事曆同步至SharePoint』流程每次執行仍會將每一天的預約視為全新紀錄，導致『公務車借用前Teams通知與回覆』流程對同一人、同一台車連續發送多張通知卡片，要求借用人重複填寫共乘人數與規範確認。此為借用行為本身正常、與 v0.3.4 追加說明的 Bug#1（同一紀錄尚未回覆前被重複發送）為不同情境，不應混為一談。

2026-08-06 曾就此情境設計「自動延用」邏輯（自動承接前次共乘人數與規範確認、不再逐日通知借用人），並完成實作與儲存驗證。2026-08-07 專案負責人審閱後確認：**借用人每日搭乘的共乘人數可能不同，需要保留每日填寫的機制**，因此「自動延用」設計不採用，已將相關流程動作全部移除，回復為每日各自發送借用人 Teams 確認卡片（比照現行既有卡片功能，卡片內容與欄位皆未變更）。

### 本次變更內容（2026-08-07）

・ 於『公務車行事曆同步至SharePoint』流程「條件3」（判斷是否為同一借用人＋同一車輛之連續借用）True 分支中，移除 2026-08-06 新增的兩個動作：
  1. **更新項目3**（SharePoint Update item，原用於承接前次共乘人數／規範確認並寫入「不需通知」＋「已完成確認」）。
  2. **在聊天室或管道中張貼訊息**（Teams，原用於發送 FYI 訊息至團隊 `ALP_TW_AD`／頻道 `General`）。
・ 移除後「條件3」True／False 分支皆為 0 動作，條件節點本身保留於流程中（不影響其餘既有邏輯），僅不再執行任何自動延用相關的自動化行為。
・ 儲存流程，Power Automate 顯示「您的流程已準備就緒。建議您對其進行測試。」，確認零錯誤。
・ 影響：同一人連續多日借用同一台車時，系統行為回復為與一般借用相同——每日各自產生 SharePoint 紀錄，並由『公務車借用前Teams通知與回覆』流程逐日發送一張 Teams 確認卡片，要求借用人每日填寫（或確認）當日共乘人數與規範確認，不再自動延用前一日資訊。
・ SharePoint 欄位 `自動延用確認次數`（Task #104 建立）目前不再由任何流程動作寫入，暫予保留、無作用中邏輯引用；是否清除該欄位留待後續視需要評估，非本次範圍。
・ 本次變更不影響 v0.3.4 的 Bug#1（重複發卡）修正邏輯，該部分維持已完成狀態。

### 已知限制與待辦

| 項目 | 狀態 | 備註 |
|---|---|---|
| 自動延用邏輯（更新項目3＋FYI訊息） | 已於 2026-08-07 移除 | 因借用人共乘人數需逐日填寫，經專案負責人確認不採用此設計 |
| 逐日發送借用人確認卡片（原設計） | 維持現行運作 | 卡片內容與欄位未變更，僅回復為每日各自發送、不自動延用 |
| SharePoint 欄位「自動延用確認次數」 | 保留、無作用中邏輯引用 | 是否清除留待後續評估 |
| Bug#1（重複發卡）修正 | 已完成，儲存驗證零錯誤 | 詳見 v0.3.4 章節，尚待端到端測試 |

### Master 判定

・ 循環預借「自動延用」邏輯（Task #107）經專案負責人審閱後**決定不採用**，相關流程動作已於 2026-08-07 完整移除並儲存驗證零錯誤；流程回復為每日發送借用人確認卡片的原設計。
・ 此變更與 v0.3.4 的 Bug#1 修正相互獨立，Bug#1 修正邏輯不受影響、維持已完成狀態。
・ 依專案治理原則（測試階段，未經明確同意不啟用），兩流程維持關閉，待專案負責人審閱 Bug#1 修正結果後，才決定是否重新開啟排程。

### 下一階段工作

1. 兩流程經負責人同意後，安排端到端測試：重點驗證 Bug#1 修正是否解決重複發卡問題；循環預借情境下應如一般借用一樣逐日正確發送確認卡片（不涉及自動延用）。
2. SG-T09 測試資料清理完成後，會同 Bug#1 修正結果一併提交正式上線驗收確認。
3. 更新 README、CHANGELOG、todo 反映本次設計變更，並依專案版本控管規則透過 Chrome 瀏覽器同步至 GitHub。

## v0.3.6 Master 更新 - 補上通知發送時間寫入邏輯，拆分重複資料檢查結果欄位

更新日期：2026-08-08

### 背景

專案負責人於 SG-T05 獨立測試案例補齊、SG-T09 測試資料清理後，確認接續處理兩項已記錄在案的設計缺口（原列於 `docs/todo.md` 與 `docs/incident-handling-guide.md` 已知限制清單）：

1. `通知發送時間` 欄位雖已於 SharePoint 清單建立，但從未被任何 Power Automate 動作寫入，僅能以 `通知狀態` 欄位間接反映是否已通知，無法精確稽核發送時間點。
2. `重複資料檢查結果` 欄位同時承載 FT-113（同車時段重疊）與 SG-T09（唯一鍵重複）兩種不同語意的判斷結果，後執行動作（SG-T09 分支）會覆蓋前一判斷結果（FT-113），承辦人無法單從欄位值判斷異常屬於哪一種情境。

專案負責人確認依既定「先清資料，再修設計缺口，最後端到端測試」順序，於 2026-08-08 授權處理以上兩項（Batch 2），Bug#1 端到端測試（Batch 3）維持延後，待另行確認。

### 本次變更內容

**通知發送時間寫入邏輯**

・ 於『公務車借用前Teams通知與回覆』流程（Flow ID `8187f981-d4a8-4ca5-89a7-684f69c278f5`）既有的送卡前鎖定動作「更新項目1」新增一個欄位對應：`通知發送時間 = @{utcNow()}`，與既有 `通知狀態 = 已通知` 鎖定寫入合併於同一動作，無需新增動作。
・ 採用專案既有的原始 UTC 時間戳慣例（與 `事件最後修改時間` 等欄位一致）。

**重複資料檢查結果欄位拆分**

・ 於 SharePoint List `公務車借用管理` 新建 Choice 欄位 `同車時段重疊檢查結果`（選項：正常／異常，預設正常），專供 FT-113 使用。
・ 以 REST API 遷移既有 16 筆正式紀錄：其中 2 筆帶有歷史 FT-113 `異常` 標記的紀錄（SharePoint ID 20、22）移至新欄位、原欄位重設為 `正常`；其餘 14 筆新欄位直接設為 `正常`。
・ 修正『公務車行事曆同步至SharePoint』流程（Flow ID `b6d1ec5c-0fc5-46c1-85b0-d8d68c72c0ce`）「套用至各項1」內的「更新項目」與「建立項目」兩動作：FT-113 同車時段重疊判斷式 `if(greater(length(body('篩選陣列')),0),'異常','正常')` 改寫入新欄位 `同車時段重疊檢查結果`，原 `重複資料檢查結果` 欄位改為固定寫入 `正常`。
・ 複核並確認 SG-T09 分支「更新項目2」（條件2→套用至各項3）維持完全不變，僅寫入 `重複資料檢查結果 = 疑似重複`，兩分支邏輯清楚分離，無交互影響。
・ 擴充『公務車借用前Teams通知與回覆』流程頂層發送條件（原 8 項 AND 判斷式），新增第 9 項：`同車時段重疊檢查結果 不等於 異常`，確保 FT-113 標記異常的紀錄比照既有 SG-T09 標記疑似重複的紀錄，一致被排除於 Teams 通知對象之外，維持「不影響通知排除邏輯」的既有保證。
・ 於 SharePoint「所有項目」檢視加入新欄位 `同車時段重疊檢查結果` 為可見欄位，比照既有 `重複資料檢查結果` 欄位處理，供承辦人查看判斷。

**驗證**

・ 兩流程儲存皆確認「您的流程已準備就緒」，零錯誤（僅既有無關聯的「取得多個項目 缺少 OData 篩選查詢」警告）。
・ 未執行端到端手動觸發測試（Batch 3，維持延後）；本次驗證範圍為流程設計時檢查與資料遷移正確性確認。

### 已知限制與待辦

| 項目 | 狀態 | 備註 |
|---|---|---|
| 通知發送時間欄位共用問題 | 已解決 | 與既有「通知狀態」鎖定同一動作寫入，未新增動作 |
| 重複資料檢查結果欄位共用問題 | 已解決 | 新增 `同車時段重疊檢查結果` 獨立欄位，SG-T09 分支不受影響 |
| SharePoint 欄位「自動延用確認次數」 | 沿用 v0.3.5 狀態，保留、無作用中邏輯引用 | 非本次範圍 |
| Bug#1（重複發卡）端到端測試 | 尚未執行 | 專案負責人指示先完成本輪設計缺口修補，端到端測試（Batch 3）另行確認後再進行 |

### Master 判定

・ Task #126（通知發送時間欄位寫入邏輯）、Task #127（拆分重複資料檢查結果欄位）皆已完成並儲存驗證零錯誤。
・ 依「先清資料，再修設計缺口，最後端到端測試」既定順序，Batch 2（設計缺口修補）至此完成；Batch 3（Bug#1 端到端測試）維持延後，待專案負責人另行確認後再開始。
・ 依專案治理原則（測試階段，未經明確同意不啟用），兩流程維持關閉。

### 下一階段工作

1. 待專案負責人確認後，安排 Batch 3：Bug#1 端到端測試，並一併驗證本次新增之 `同車時段重疊檢查結果` 欄位與第 9 項通知排除條件於實際排程觸發下運作正確。
2. SG-T09 測試資料（TEST-SGT09-A；SharePoint ID 19）清理完成後，會同上述測試結果一併提交正式上線驗收確認。
3. 更新 README、CHANGELOG、todo 反映本次變更，並依專案版本控管規則透過 Chrome 瀏覽器同步至 GitHub。

## v0.3.7 Master 更新 - 差異確認決策清單逐項複查與實作，意外發現同步流程停用19天

更新日期：2026-08-27

### 背景

專案負責人確認依「小項目優先」順序，逐一複查並落實 `docs/company-account-and-booking-rules-change-decisions.md` 差異確認決策清單中列為待實作的 #128～#134（對應決策清單一～四節）。複查原則：不直接信任任務追蹤清單的「已完成／待處理」標籤，一律先以程式碼層級或官方文件查證，再決定是否需要實際修改。

### 本次變更內容

**#131 借用人判斷（技術查證，非程式碼變更）**

・ 查證 Microsoft Graph `event` 資源官方屬性清單（[learn.microsoft.com/graph/api/resources/event](https://learn.microsoft.com/en-us/graph/api/resources/event)），確認 `event` 物件並無獨立 `createdBy` 欄位，僅有 `organizer`（型別 `Recipient`）可代表「此行事曆事件由誰建立/寄送」；`createdDateTime` 僅為時間戳記，不含身分資訊。
・ 因「不允許代訂」已是既定業務規則，正常流程下 `organizer` 即等同預約建立者本人，無其他技術替代方案，故維持現況（不更動程式碼），已於決策文件補充技術查證說明並結案。

**#129 共乘人數欄位改名與驗證（複查確認已實作）**

・ 複查『公務車借用前Teams通知與回覆』流程 Adaptive Card JSON，確認欄位已改名為「乘車總人數（含駕駛）」（`carpoolCount`，`Input.Number`，`min: 0`）。
・ 複查條件式，確認已有 `greaterOrEquals(body('剖析_JSON')?['carpoolCount'], 0)` 與 `equals(string(carpoolCount), string(int(carpoolCount)))` 組成的後端非負整數驗證，可擋下負數、小數與非數字文字，非待實作項目。

**#130 Card 到期/失效訊息優先順序（複查確認已實作）**

・ 複查『公務車借用前Teams通知與回覆』流程條件1 False 分支的 `messageBody` 巢狀 `if(or(...))` 邏輯，確認已依「已取消 > 已完成確認/已領鑰 > 已開始 > 內容異動 > 版本過期」優先順序回覆對應提示訊息，與決策清單第三節規則完全相符，非待實作項目。

**#132 換車邏輯（架構分析為主，live 測試受限）**

・ 架構分析：SharePoint 唯一鍵（資源信箱 mailbox ＋ 行事曆事件 ID）天然依 mailbox 區分車輛，換車情境下（取消舊車事件＋於新車資源信箱建立新事件）新事件會被視為全新借用，既有取消偵測邏輯（v0.2.15）與 SG-T09 唯一鍵重複偵測（v0.3.3）已足以覆蓋，且循環延用邏輯已於 v0.3.5 移除、不會有殘留資料延用問題，判定無需新增程式碼。
・ Live 測試：於 Outlook 建立 Altis／Camry 兩筆合成換車測試預約，皆因「位置無法使用」被資源信箱自動拒絕（已拒絕:1），未寫入資源行事曆，因此無法被同步流程讀取，測試未能取得直接證據；經與專案負責人確認後，接受架構分析作為結案依據，兩筆測試邀請已取消清理。

**#133 Graph 查詢時間窗調整（已完成並驗證）**

・ 修正『公務車行事曆同步至SharePoint』流程（Flow ID `b6d1ec5c-0fc5-46c1-85b0-d8d68c72c0ce`）「傳送 HTTP 要求」動作 Uri 運算式，起始邊界由 `utcNow()` 改為 `addHours(utcNow(),-24)`，結束邊界維持 `addDays(utcNow(),14)`，符合決策清單「往前 24 小時～往後 14 天」規則。
・ 過程中定位出先前多次運算式編輯未成功寫入的根因：Power Automate 運算式編輯器（fx 彈出視窗）需在輸入完成後按 **Tab 使輸入框失焦**，再點擊「更新」按鈕，否則最後輸入內容不會被讀取寫入；此為可重用的操作技巧。
・ 已透過程式碼檢視、流程層級儲存、硬性重新整理頁面三重確認寫入持久化成功，並於本次重新開啟排程手動執行時，以 SharePoint 新增資料涵蓋至 9/7（符合 14 天窗口）佐證新查詢時間窗已生效於正式環境。

**#134 兩流程失敗告警（部分驗證）**

・ 複查『公務車行事曆同步至SharePoint』流程，確認關鍵動作已具備 `runAfter` 含 `Failed` 分支的失敗告警設計。
・ 『公務車借用前Teams通知與回覆』流程對應複查因 Power Automate 網頁本次工作階段末端持續出現 `Network Error`（多次重新整理、切換分頁、重新連線瀏覽器擴充功能皆未解決，判斷非本機瀏覽器連線問題）而未能完成，**此項暫列為未完全驗證，待下次可正常存取 Power Automate 時補查**。

**意外發現：同步流程停用19天**

・ #132 live 測試期間，發現核心流程『公務車行事曆同步至SharePoint』狀態顯示為「關閉」，28 天執行歷程中最後一次成功執行為 8/8 上午，即已在未告知的情況下停用 19 天，導致 SharePoint 借用資料自 8/8 起未再更新（僅有 8/17～8/20 之前批次同步的過期資料），期間無法偵測任何新增、取消或異動。
・ 已將此發現、影響範圍與處置選項回報專案負責人，經同意後於 2026-08-27 重新開啟排程並手動執行一次，確認 SharePoint 已補回同步至 9/7 的最新借用資料，同時驗證 #133 的新查詢時間窗已生效。
・ 停用期間查無相關異動或事故回報，惟已提醒專案負責人留意日常維護說明中「定期檢查流程執行狀態」項目的落實。

**測試資料清理**

・ #132 live 測試產生的 Outlook 測試邀請（Altis「TEST-換車測試」、Camry「TEST-換車測試-新車-請忽略」）皆已於 2026-08-27 取消。

### 已知限制與待辦

| 項目 | 狀態 | 備註 |
|---|---|---|
| #134 Teams 通知流程失敗告警 | 待補查 | Power Automate 網頁本次連線異常，待恢復後確認 |
| SG-T07（重複觸發） | 原「通過」結果撤回，待重新驗證 | 詳見 `test-cases/function-test-plan.md`，對應 #137 |
| Bug#1（重複發卡）端到端測試 | 尚未執行 | 沿用 v0.3.6 狀態 |
| SG-T09 測試資料清理（SharePoint ID 19） | 尚未清理 | 沿用既有狀態，專案負責人自行清理 |

### Master 判定

・ Task #128～#133 已全數完成並確認結案（含技術查證、複查確認既有實作、程式碼修正並驗證持久化）。
・ Task #134 部分完成：同步流程已確認，Teams 流程待 Power Automate 恢復存取後補查。
・ 意外發現的同步流程停用問題已修復並驗證，不列為獨立 Task 編號，併入本版本記錄。
・ 依專案治理原則，Teams 通知流程維持關閉，同步流程已恢復排程運作。

### 下一階段工作

1. Power Automate 網頁恢復正常存取後，補查『公務車借用前Teams通知與回覆』流程的 `runAfter=Failed` 失敗告警設計，完成 #134。
2. 安排 #137：SG-T07 專門驗證（Bug#1 修正後 Teams 發卡是否仍會重複），驗證通過後評估是否可重新開啟 Teams 通知流程。
3. 安排 #135：實測 SharePoint 即時同步之 Power Apps 可行性。
4. 安排 #136：循環預約功能完整實作（本專案最大工作量項目）。
5. 更新 README、CHANGELOG、todo 反映本次變更，並依專案版本控管規則透過 Chrome 瀏覽器同步至 GitHub。
