# 待辦事項
更新日期：2026-08-27（v0.3.7）
## P0 必須先完成

1. 不再要求以 Exchange Administrator 登入流程；使用 `ad.general@alp.global` 與 Office 365 Outlook 標準連線。
2. 已完成 Outlook 明確顯示 Altis、Camry、Cross 三台共用行事曆後重測；`取得行事曆 (V2)` 仍只回傳個人 Calendar。
3. [已解除] 曾評估將三台行事曆資料夾權限調整為 Editor 或提升為 Full Access；v0.2.11 已證實不論何種權限，`取得行事曆 (V2)`／`取得事件的行事曆檢視 (V3)` 皆無法讀取 Resource Mailbox，屬連接器動作限制。
4. [已由新方案取代] 不需要再找 Calendar ID；改用「傳送 HTTP 要求」直接以資源信箱 Email 呼叫 `GET /v1.0/users/{mailbox}/calendar/events`。
5. [已由新方案取代] 不使用 `取得事件的行事曆檢視 (V3)`；改用「傳送 HTTP 要求」+ `calendar/events` + `$filter` 做時間區間查詢。
6. [已完成] 對三台車分別驗證：主旨、起始時間、結束時間、是否刦全天、建立者／借用人、Event ID 與 iCalUId；ATA-9627 已於 v0.2.11 驗證成功，Camry、Cross 已於 v0.2.12 前完成驗證，三台車皆採用相同 HTTP 直連 Graph 路徑。
7. 確認資源信箱取消與時間異動是否能被 Power Automate 偵測。
8. 確認所有流程日期時間均轉為 `Asia/Taipei`。
9. 確認 Event ID / iCalUId 唯一識別邏輯可用。
10. 確認舊 Teams Adaptive Card 回覆前防呆檢查可用。
11. 確認 Flow Concurrency Control 與重複資料防止規則可用。

## P1 正式流程開發

1. [x] 建立 `公務車行事曆同步至 SharePoint` 流程（v0.2.12 已完成並開啟排程）。
2. [x] 設定三台資源信箱同步邏輯（共用同一組 Apply to each + HTTP 要求 + 解析 + 寫入）。
3. [x] 以 `預約唯一鍵` 避免重複建立項目（取得多個項目 + 條件判斷新增或更新）。
4. [x] 建立 `是否整天` 判斷。
5. [x] 建立 `預計通知時間` 計算。
6. [x] 建立取消預約更新為 `取消`（v0.2.15 已實作事件取消偵測補強邏輯並驗證通過）。
7. [ ] 建立時間異動後重新計算通知時間（尚未驗證異動情境）。
8. [x] 在正式流程中寫入並維護已建立的防呆欄位，例如 iCalUId、事件同步狀態、卡片版本、通知狀態、處理鎖定狀態（欄位已寫入，實際異動情境待測試）。
9. [x] 啟用 Concurrency Control，避免重複同步（v0.2.16 已於 Recurrence 觸發程序完成）。

## P2 Teams 通知與回覆

・ [x] 建立 `公務車借用前 Teams 通知與回覆` 流程（v0.3.0 已完成）。
・ [x] 以 `預計通知時間` 判斷是否發送。
・ [x] 發送 Adaptive Card 給 `借用人 Email`。
・ [x] 寫入 `通知發送時間`（v0.3.6 已完成：於送卡鎖定動作「更新項目1」新增 `@{utcNow()}` 欄位對應）。
・ [x] 接收借用人回覆。
・ [x] 寫入 `共乘人數`、`規範確認狀態`、`Teams 回覆時間`。
・ [x] 完整填寫時更新為 `已完成確認`。
・ [x] 未完整填寫時維持或更新為 `未完成填寫`（以 Adaptive Card 必填/必勾選機制於卡片端阻擋，確保不會送出不完整資料）。
・ [x] 回覆寫回前ꪢ查 Event ID、卡片版本、事件同步狀態（SG-T06 條件1，8 項判斷式）。
・ [x] 事件已取消、已失效或已異動時，提示借用人重新確認最新資訊（v0.3.0 已完成並驗證通過，詳見 `release-notes/v0.3.0.md`）。

## P3 測試與驗收

1. 測試一般借用。
2. 測試早上 08:30 借用，通知時間應為 08:00。
3. 測試 08:00 前借用，通知時間應為 08:00。
4. 測試整天借用，通知時間應為 08:00。
5. [x] 測試 Outlook 預約取消（v0.2.15 已通過）。
6. 測試 Outlook 預約時間異動。
7. 測試未填共乘人數。
8. 測試未勾選規範確認。
9. 測試承辦人後台判斷是否可領鑰。
10. 測試時區轉換與 UTC 原始時間不直接顯示。
11. 測試全天事件不會產生前一天 23:00 通知。
12. 測試舊 Teams Adaptive Card 送出不得覆蓋新資料。
13. 測試重複觸發不會重複建立或重複通知。

## P4 文件與上線

1. [x] 建立行政、總務操作手冊（`docs/operation-manual.md`，v0.3.4 已完成）。
2. [x] 建立異常處理說明（`docs/incident-handling-guide.md`，v0.3.4 已完成）。
3. [x] 建立日常維護說明（`docs/maintenance-guide.md`，v0.3.4 已完成）。
4. [x] 完成上線前驗收紀錄（`test-cases/go-live-acceptance-record.md`，v0.3.4 已完成）。
5. [x] 視需要建立 Git tag 與正式 release note（v0.2.12～v0.3.3 已透過 GitHub 網頁 UI 補齊）。
6. [x] 清理 FT-113 測試資料（2026-08-04 已完成，SharePoint ID 13/14/16/17 與 Outlook 全部測試事件皆已確認清除）。
7. [ ] 清理 SG-T09 測試資料（TEST-SGT09-A；SharePoint ID 19、21）——專案負責人自行清理。
8. [ ] 清理完成後，完成正式上線驗收確認（現已同時取決於下方第 9 項 Bug 修正完成）。
9. [x] 修正「公務車借用前Teams通知與回覆』流程重複發送通知 Bug#1（2026-08-06 員工回報發現，根因與修正方向詳見 `docs/incident-handling-guide.md` 與 CHANGELOG v0.3.4 追加說明；邏輯已實作完成）：
   - [x] 於發送 Adaptive Card 前（張貼調適型卡片並等候回覆動作之前）即寫入「通知狀態＝已通知」作為送卡鎖定，避免等候回覆期間被下一次排程重複比對送出。
   - [x] 為 Recurrence 觸發程序補上並行控制（限制＝1），與同步流程一致，避免並行執行個體重疊處理同一筆紀錄。
   - [x] 檢視 SG-T06 條件1 False 分支的固定回覆訊息，改為明確說明重複卡片情境，而非容易誤導的「此借用已取消或已變更」。
   - [ ] 修正並儲存後，先以測試紀錄手動觸發驗證不再重複發送，再重新開啟兩個流程（目前皆為關閉／測試階段，未經使用者同意不重新啟用）。
10. [x] 評估循環預借「自動延用」邏輯後決定不採用（2026-08-0）：因借用人�Xy�亚數需逐日填寫，維持逐日發送借用人 Teams 確認卡片之原設計，比照現行既有卡片功能；已將原於『公務車行事曆同步至SharePoint』流程「條件3」True 分支新增之更新項目3（承接前次共乘人數／規範確認、寫入不需通知＋已完成確認）與 Teams FYI 訊息動作全部移除，流程已儲存姪驗證零錯誤。詳見 `docs/project-master-record.md` v0.3.5 章節。
    - [ ] 兩流程經負責人同意重新開啟後，安排端到端測試：重點驗證 Bug#1 修正是否解決重複發卡問題，並確認循環預借情境下仍逐日正確發送確認卡片（不涉及自動延用）。
## v0.2.7 待辦事項 - ATA-9627 V3 事件讀取

1. 建立或開啟 Power Automate 測試流程 `公務車功能測試-ATA9627事件讀取`。
2. 在 `取得事件的行事曆檢視 (V3)` 的 Calendar Id 填入 `6049e1d1-b34c-4cca-b530-2c7c4b77abe9`。
3. 查詢期間設定為 `utcNow()` 至 `addDays(utcNow(),7)`。
4. 執行測試並確認是否可讀到 `subject = TEST` 或 ATA-9627 未來 7 天事件。
5. 若成功，記錄 Event ID 與 iCalUId。
6. 若失敗，保留完整錯誤訊息並分類原因。
7. 測試完成後更新 `docs/ata9627-v3-calendar-event-test.md`、README、CHANGELOG 與 Master 紀錄。

## v0.2.8 待辦事項 - ATA-9627 Calendar ID 已取得後測試

1. 將 `公務車功能測試-ATA9627事件讀取` 的 V3 Calendar Id 改為 `6049e1d1-b34c-4cca-b530-2c7c4b77abe9`。
2. 按「測試」並選擇手動執行。
3. 檢查 V3 輸出是否有事件陣列。
4. 若有事件，確認是否包含 subject、start、end、isAllDay、organizer / creator、id、iCalUId。
5. 將成功或失敗結果回填至 `docs/ata9627-v3-calendar-event-test.md`。

## v0.2.9 待辦事項 - ATA-9627 V3 測試實作版

1. 依 `power-automate/ata9627-v3-test-implementation.md` 建立或調整 `公務車功能測試-ATA9627事件讀取`。
2. 確認 V3 動作名稱為 `Get_ATA9627_Events_V3`。
3. 確認 Calendar Id 為 `6049e1d1-b34c-4cca-b530-2c7c4b77abe9`。
4. 確認已加入 `Filter_TEST_Events`、`Compose_All_Event_Count`、`Compose_TEST_Event_Count`、`Compose_First_Event_Raw`、`Select_Event_Field_Check`。
5. 執行手動測試。
6. 若可讀到事件，回填 Event ID、iCalUId、起訖時間與是否全天。
7. 若未讀到 TEST，但全部事件數量大於 0，確認 TEST 是否在未來 7 天內。
8. 若 V3 失敗，保留完整錯誤訊息並判斷是否為 Calendar ID、權限、連接器限制或查詢期間問題。
9. V3 成功後，建立 `公務車行事曆同步至 SharePoint - ATA9627 MVP`。

## v0.2.10 待辦事項 - ATA-9627 候選 GUID 實測後

1. [x] 將候選 GUID `6049e1d1-b34c-4cca-b530-2c7c4b77abe9` 實際送入 V3。
2. [x] 保存 `400 BadRequest / ErrorInvalidIdMalformed / The Id is invalid.` 與執行追蹤資訊。
3. [x] 停用目前每分鐘執行的 `公務車功能測試-ATA9627事件讀取`；2026-07-23 確認狀態已為「關閉」。
4. [x] 評估連接器可列舉來源；確認 `取得行事曆 (V2)`／`取得事件的行事曆檢視 (V3)` 皆無法讀取 Resource Mailbox，屬連接器限制。
5. [x] 評估 O365 E1 可行且不使用 Premium 的替代方案；已找到「傳送 HTTP 要求」直連 Graph 的可行路徑。
6. [x] 已成功取得事件與 Event ID / iCalUId（`calendar/events` + `$filter`），可進入 SharePoint 同步 MVP。

## v0.2.11 待辦事項 - Resource Calendar 讀取阻擋解除後

1. [x] IT 提供 Full Access 委派後，重測 `取得行事曆 (V2)` 與 `取得事件的行事曆檢視 (V3)`。
2. [x] 驗證 `取得電子郵件 (V3)` 的 `原始信箱地址` 參數可指定 Resource Mailbox。
3. [x] 找到並實測「傳送 HTTP 要求」+ `calendar/events` + `$filter` 的可行路徑，`statusCode 200`。
4. [x] 對 Camry（`room_nhb4_car_camry@alp.global`）重複同樣的 HTTP 要求驗證。
5. [x] 對 Cross（`room_nhb4_car_cross@alp.global`）重複同樣的 HTTP 要求驗證。
6. [x] 建立正式流程 `公務車行事曆同步至 SharePoint`，三台車共用同一組讀取、解析、寫入邏輯。
7. [x] 依 `預約唯一鍵`（資源信箱 + 行事曆事件 ID）判斷新增或更新，避免重複建立。
8. [x] 計算 `是否整天`、`預計通知時間`，並將 UTC 時間轉換為 `Asia/Taipei` 顯示。
9. [x] 寫入既有防呆欄位：iCalUId、事件同步狀態、事件最後修改時間、原始開始/結束時間 UTC。
10. [ ] 刪除暫存測試流程 `公務車功能測試-郵件動作參數檢查(可刪除)`（待正式流程通過端到端測試後）。
11. [x] 完成後更新 CHANGELOG、Master 紀錄，並依階段版本規則建立下一版本記錄（v0.2.12）。

## v0.2.12 待辦事項 - 正式流程建立並啟用排程後

1. [x] 完成「更新項目」「建立項目」兩分支欄位對應，並修正 Apply to each 命名空格轉底線的參照錯誤。
2. [x] 儲存流程確認囶錯誤（「您的流程已準備就緒」）。
3. [x] 開啟流程，排程狀態由「關閉」變更為「開啟」，流程檢查程式錯誤 0、警告 0。
4. [ ] 執行端到端測試流程：實際於 Outlook 建立／修改／取消預約，確認 15 分鐘內正確同步至 SharePoint。
5. [ ] 測試 P3 各項情境（整天借用、早上過早借用、取消、時間異動、重複觸發等）。
6. [ ] 端到端測試通過後，將 `是否測試資料` 由 `是` 切換為正式運作邏輯。
7. [ ] 刪除暫存測試流程 `公務車功能測試-郵件動作參數檢查(可刪除)`。
8. [ ] 建立 Concurrency Control（Degree of Parallelism），降低重複同步風險。
9. [ ] 進入 P2 階段，建立「公務車借用前 Teams 通知與回覆」流程。

## v0.2.13 待辦事項 - 執行期錯誤修正並完成首次成功執行後

1. [x] 修正兩處 Apply to each 迴圈 `foreach` 大括號運算式錯誤。
2. [x] 修正「取得多個項目」篩選查詢欄位內部名稱錯誤（改用 SharePoint REST API 驗證後的 `OData_` 前綴內部名稱）。
3. [x] 修正「條件」判斷式 `greater` 比較大括號運算式錯誤。
4. [x] 修正「建立項目」「更新項目」的「是否整天」欄位型別錯誤（改為原生布林參照）。
5. [x] 手動觸發測試，確認運行紀錄首次出現「測試成功」，並逐層展開執行追蹤確認三台車與 SharePoint 寫入皆成功。
6. [x] 以 SharePoint REST API 直接查詢清單，確認資料正確寫入且無重複項目。
7. [x] 執行 P3 情境測試時發現「更新項目」時間欄位 +8 小時位移嚴重錯誤（詳見 v0.2.14 待辦事項）。
8. [x] 確認「我的流程」清單中是否有本流程的重複項目需清理（v0.2.16 已完成）。
9. [x] 端到端測試通過後，將 `是否測試資料` 由 `是` 切換為正式運作邏輯，並刪除暫存測試流程、建立 Concurrency Control（v0.2.16 已完成）。

## v0.2.15 待辦事項 - 事件取庈偵拜補強邏輯完成後

1. [x] 新流程中新增陣列變數 `CurrentEventKeys`，記錄每次執行實際讀取到的所有事件唯一鍵。
2. [x] 新增 SharePoint 取得多個項目動作，篩選該诋輛「未取庈」且「借用起始時間」落在未次讠取時間窗内的既有紀錄。
3. [x] 新增巢狀迴圈與條�件判斷，比對有文錄唯一鍵是否仍存在本次讀取結果中。
4. [x] 新增更新項目動作，將唯一鍵消失的既有紀錄標記為 `已取消 = 是`、`事件同步狀態 = 已取消`、`取消時間 = utcNow()`。
5. [x] 儲存流程確認零錯誤，手動觸發測試，以 SharePoint REST API 驗證：既定測試案例（Id=4）正確標記已取消；額外發現並正確標記一筆先前測試階段遺留的孤兒紀錄（Id=1）；仍然有效的未來借用（Id=2、Id=3）未受影響。
6. [x] 更新 `test-cases/function-test-plan.md`（FT-103、SG-T04 狀態改為通過）、`docs/project-master-record.md`、`CHANGELOG.md`。
7. [x] 確認「我的流程」清單中是否有本流程的重複項目需清理（v0.2.16 已完成，詳見下方 v0.2.16 待辦事項）。
8. [x] 端到端測試通過後，將 `是否測試資料` 由 `是` 切換為正式運作邏輯，並刪除暫存測試流程、建立 Concurrency Control（v0.2.16 已完成）。
9. [ ] 進入 P2 階段，建立「公務車借用前 Teams 通知與回覆」流程。

## v0.2.16 待辦事項 - 流程清理、正式化切換與 Concurrency Control 完成後

1. [x] 檢視「我的流程」清單，發現重複流程（Flow ID `a3357db1-...`）與兩個文件記載已關閉、實際仍在背景執行的測試流程（`公務車功能測試-資源信箱 讀取`、`公務車功能測試-SharePoint清�ꀣ線`），確認動作結構不涉及 SharePoint 寫入後予以關閉。
2. [x] 刪除暫存測試流程 `公務車功能測試-郵件動作參數檢查(可刪除)`。
3. [x] 將「更新項目」「建立項目」兩處 `是否測試資料` 由 `是` 改為 `否`，儲存流程確認零錯誤。
4. [x] 在 Recurrence 觸發程序啟用並行控制，平行處理原則的程度設為 1；確認三個 Apply to each 迴圈預設即為循序執行，無需調整。
5. [ ] 視需要決定既有測試紀錄（Id=1～4）的 `是否測試資料` 是否批次修正為 `否`。
6. [ ] 進入 P2 階段，建立「公務車借用前 Teams 通知與回覆」流程。


## v0.3.0 待辦事項 - P2 Teams 通知與回覆流程完成並驗證通過後

・ [x] 建立「公務車借用前Teams通知與回覆」流程外殼與 SharePoint 取得項目篩選待通知紀錄。
・ [x] 加入 Apply to each 並發送 Adaptive Card 給借用人。
・ [x] 實作回覆解析與 SG-T06 防呆檢查邏輯（條件1，8 項判斷式）。
・ [x] 寫回 SharePoint 更新欄位（共乘人數、規範確認、領鑰狀態、通知狀態、Teams 回覆時間）。
・ [x] 修正「取得項目」action 的 `id` 參數 null 值 Bug。
・ [x] 修正 SG-T06 條件1 語法錯誤（缺少 `@` 前綴、括號存取子誤寫）與 `cardVersion` 型別不一致（改用 `string()` 包裝）兩類 Bug。
・ [x] 手動觸發流程並確認 FT-108（Teams 卡片正確送出）。
・ [x] 使用者於 Teams 回覆卡片，確認 FT-109～FT-111。
・ [x] 測試 SG-T06 正向路徑（卡片資料與最新記錄一致，正確更新 SharePoint）。
・ [x] 補齊 FT-104～FT-107（通知時間規則說明：09:30/08:30/早於 08:00/整天適用之`預計通知時間`計算）、FT-112（承辦人後台依領鑰狀態判斷）、SG-T08（Event ID 唯一鍵）等測試案例，均已執行並確認通過
・ [x] FT-113（同車重複預約）已完成測試並確認現行流程未強制避免，判定為未通過／已知限制，待後續評估補強方案
・ [ ] SG-T09（重複資料異常標記）尚末實際執行，需安排人為建立重複預約唯一鍵資料進行驗證
・ [ ] 補上「通知發送時間」欄位的實際寫入邏輯（目前僅以「通知狀態」欄位反映）
・ [ ] 進入 P4 階段：建立行政/總務操作手冊、異常處理說明、日常維護說明，完成上線前驗收紀錄

## v0.3.1 待辦事項 - P2 收尾測試補齊完成後

・ [x] 執行並驗證 FT-104～FT-107 通知時間邊界條件均通過，並已清除對應測試事件與紀錄。
・ [x] 執行並驗證 FT-112（承辦人後台領鑰判斷）、SG-T08（Event ID 唯一鍵），均通過。
・ [x] 測試並確認 FT-113（同車重複預約）目前未通過，列為已知限制：現行流程以 Editor 委派權限直接建立事件，未透過會議邀請，不會觸發 Outlook 資源信箱原生雙重預約阻擋機制。
・ [x] 評估並實作 FT-113 補強方案：於流程內新增同車時段重疊檢查邏輯，發現重疊時標記 `重複資料檢查結果 = 異常`，不阻擋建立（v0.3.2 已完成，詳見下方 v0.3.2 待辦事項）。
・ [x] 安排執行 SG-T09：人為建立相同 `預約唯一鍵` 的多筆資料作為模擬，驗證系統是否正確標記重複異常且不發送 Teams 通知（v0.3.3 已完成）。
・ [x] 補上「通知發送時間」欄位實際寫入邏輯（v0.3.6 已完成）。
・ [ ] 進入 P4 階段，建立行政/總務操作手冊、異常處理與日常維護說明，完成上線前驗收紀錄。

## v0.3.2 待辦事項 - FT-113 補強邏輯完成並驗證通過後

・ [x] 於「套用至各項 1」新增第二個「取得多個項目」，篩選同一台車、未取消、不同預約唯一鍵的既有紀錄。
・ [x] 新增「篩選陣列」比對借用起訖時間是否與既有紀錄重疊（「建立項目」「更新項目」皆依結果寫入 `重複資料檢查結果`。
・ [x] 發現並修正「建立項目」與「更新項目」時間格式不一致，導致標記於排程自動觸發（Update 路徑）後被靜默重置回 `正常` 的隱性 Bug；統一兩動作皆輸出原始 UTC 並加 `Z` 後綴，同步簡化「篩選陣列」比對條�t。
・ [x] 建立新測試事件對（TEST-FT113-A3／B3）重新驗證，確認標記可正確跨 Create→Update 轉換維持（一筆正常、一筆異常，Update 後互換但不會雙雙重置）。
・ [x] 撰寫管理員手動確認操作程序文件 `docs/admin-manual-confirmation-procedure.md`（承辦人於借用人未透過 Teams 卡片回覆時間，如何手動於 SharePoint 後台確認並發放鑰匙，須同步更新「領鑰狀態」與「通知狀態」等欄位）。
・ [ ] 清理 本輪測試璢事的 Outlook 測試事件（TEST-FT113-A/B、A2/B2、A3/B3）與 SharePoint 測試紀錄（ID 13、14、16、17），清單詳見 `release-notes/v0.3.2.md`。
・ [x] 完排到舊測試成功（成只若高驗流程状行甌發驗塋地戽86。
・ [x] 完成後更新項目，並發恋（並恋恋，並恋恋＆。
・ [x] 手擌全跟行埞，行事曆同步事件資界。
・ [ ] 進入 P4 階段通過後

## v0.3.3 待辦事項 - SG-T09 重複資料異常標記補強邏輯完成並驗證通過後

・ [x] 於「公務車行事曆同步至SharePoint」新增條件2（比對同一 `預約唯一鍵` 既有紀錄筆數是否 >1）→ 套用至各項3 → 更新項目2（標記 `重複資料檢查結果 = 疑似重複`）。
・ [x] 修正條件2 比較左值、套用至各項3 的 foreach、更新項目2 的識別碼參數，三處運算式缺少正確 `@` 前綴或誤帶多餘 `@{}` 的雙重跳脫 Bug。
・ [x] 修正更新項目2 誤將 `已取消`／`已失效` 強制寫入 `true`，並覆蓋車輛名稱、規範確認狀態等多個無關欄位的問題，改為僅寫入 `重複資料檢查結果` 一個欄位。
・ [x] 儲存流程確認囶錯誤，以 SharePoint REST API 建立與既有測試事件相同 `p��約唯一鍵` 的人為重複紀錄（TEST-SGT09-A／B），手動觸發驗證兩筆皆正確標記 `疑似重複`。
・ [x] 複核「公務車借用前Teams通知與回覆」流程 8 項條件式，確認明確比對 `重複資料檢查結果 = 正常`，`疑似重複`／`異常`皆會被排除於通知對象之外，無需修改。
・ [ ] 清理本輪測試產生的 SharePoint 測試紀錄（TEST-SGT09-A、TEST-SGT09-B）與 Outlook 測試事件（TEST-SGT09-A，2026/8/4 14:00-14:30，於公務車9627-B4-永聯內湖辦公室資源行事曆）。
・ [x] 評估是否將 `重複資料檢查結果` 拆分為獨立欄位，避免 FT-113（時間重疊）與 SG-T09（唯一鍵重複）兩種語意共用同一欄位時互相覆蓋（v0.3.6 已完成：新增 `同車時段重疊檢查結果` 欄位專供 FT-113 使用）。
・ [x] 補上「通知發送時間」欄位實際寫入邏輯（v0.3.6 已完成）。
・ [x] 進入 P4 階段，建立行政/總務操作手冊、異常處理與日常維護說明，完成上線前驗收紀錄（v0.3.4 已完成）。

## v0.3.4 待辦事項 - P4 上線前文件補齊完成後

・ [x] 建立 `docs/operation-manual.md`、`docs/incident-handling-guide.md`、`docs/maintenance-guide.md`、`test-cases/go-live-acceptance-record.md`。
・ [x] 清理 FT-113 測試資料（TEST-FT113-A/B、A2/B2、A3/B3；SharePoint ID 13、14、16、17），2026-08-04 完成。
・ [ ] 清理 SG-T09 測試資料（TEST-SGT09-A；SharePoint ID 19、21）——專案負責人自行清理。
・ [ ] SG-T09 測試資料清理完成後，完成正式上線驗收確認並更新 Master 紀錄。

## v0.3.6 待辦事項 - 通知發送時間寫入邏輯與重複資料檢查結果欄位拆刅完成後

・ [x] 補上「通知發送時間」欄位實際寫入邏輯：於「公務車借用前Teams通知與回覆」流程送卡鎖定動作「更新項目1」新增 `通知發送時間 = @{utcNow()}` 欄位對應，與既有「通知狀態＝已通知」鎖定同一動作完成，無需新增動作。
・ [x] 拆分 `重複資料檢查結果` 欄位：新建 SharePoint 欄位 `同車時段重疊檢查結果`（選擇，正常／異常）專供 FT-113 使用；`重複資料檢查結果` 自此僅由 SG-T09（疑似重複）寫入。
・ [x] 遷移既有 16 筆正式紀錄：其中 2 筆歷史 FT-113 異常標記（SharePoint ID 20、22）移至新欄位並將舊欄位重設為 `正常`，其餘 14 筆新欄位設為 `正常`。
・ [x] 修正「公務車行事曆同步至SharePoint」流程「更新項目」與「建立項目」動作，改將 FT-113 同車時段重疊判斷結果寫入新欄位；「更新項目2」（SG-T09 分支）維持不變，僅寫入 `重複資料檢查結果`。
・ [x] 擴充「公務車借用前Teams通知與回覆」流程的頂層發送條件（新增第 9 項 AND 條件：`同車時段重疊檢查結果 不等於 異常`），確保 FT-113 標記異常的紀錄與 SG-T09 標記疑似重複的紀錄一致被排除於通知對象之外。
・ [x] 於送卡前鎖定動作一併寫入 `通知發送時間`。
・ [x] 兩流程儲存皆確認零錯誤（僅既有無關聯的「取得多個項目 缺少 OData 篩選查詢」警告）。
・ [x] SharePoint」所有項目」檢視加入新欄位 `同車時段重疊檢查結果` 為可見欄位。
・ [x] 更新 `docs/system-safeguards.md`、`docs/incident-handling-guide.md`、`test-cases/function-test-plan.md`、`CHANGELOG.md`、`docs/project-master-record.md` 反映本次變更。
・ [ ] 兩流程目前仍維持關閉（測試階段，Bug#1 端到端測試尚未執行）；待專案負責人確認後安排端到端測試，含本次新欄位與條件擴充的驗證。

## v0.3.7 待辦事項 - 差異確認決策清單（#128～#134）逐項複查與實作完成後

・ [x] #131 借用人判斷：技術查證 Graph `event` 無 `createdBy` 欄位，維持用 `organizer`，已於決策文件補充說明並結案。
・ [x] #129 共乘人數欄位改名與驗證：複查確認已實作（欄位已改名「乘車總人數（含駕駛）」，後端已有非負整數驗證），非待實作項目。
・ [x] #130 Card 到期/失效訊息優先順序：複查確認已依決策清單優先順序實作，非待實作項目。
・ [x] #132 換車邏輯：架構分析確認自然支援，live 測試因資源信箱自動拒絕合成測試預約未能完整驗證，經專案負責人同意接受架構分析結案；測試邀請已清理。
・ [x] #133 Graph 查詢時間窗調整為 -24hr～+14 天：已修正『公務車行事曆同步至SharePoint』流程並以程式碼檢視、儲存、重新整理三重確認寫入成功，重新開啟排程手動執行後以 SharePoint 新資料涵蓋至 9/7 佐證生效。
・ [x] #134（同步流程部分）：確認『公務車行事曆同步至SharePoint』關鍵動作已有 `runAfter=Failed` 失敗告警。
・ [ ] #134（Teams 流程部分）：因 Power Automate 網頁本次連線異常（`Network Error`）尚未確認，待恢復存取後補查。
・ [x] 意外發現並修復：『公務車行事曆同步至SharePoint』流程已停用 19 天（8/8～8/27），經專案負責人同意已重新開啟並手動執行一次補回同步資料。
・ [ ] SG-T07（重複觸發）原「通過」結果撤回，待安排專門驗證 Teams 發卡不重複（詳見下方 #137）。
・ [ ] 進入 #135：實測 SharePoint 即時同步之 Power Apps 可行性。
・ [ ] 進入 #136：循環預約功能完整實作（本專案最大工作量項目）。
・ [ ] 進入 #137：SG-T07 重新驗證 Teams 發卡不重複，通過後評估是否可重新開啟 Teams 通知流程。
