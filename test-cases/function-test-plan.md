# 功能測試計畫

## 測試目標

確認「M365 公務車借用自動通知與後台管理流程」符合以下條件：

- Outlook 公務車資源行事曆資料可同步至 SharePoint。
- SharePoint 後台可即時查看借用狀態。
- Teams Adaptive Card 可於正確時間發送給借用人。
- 借用人回覆後可更新 SharePoint。
- 未完成填寫時，承辦人不得發放鑰匙。
- 全流程使用 O365 E1 可用標準功能，不使用 Premium 連接器。

## 目前已完成測試

| 測試編號 | 測試項目 | 結果 | 備註 |
|---|---|---|---|
| FT-001 | Power Automate 讀取 SharePoint List | 通過 | `公務車功能測試-SharePoint清單連線` 最新執行成功 |
| FT-002 | SharePoint 欄位建立與顯示 | 通過 | 已確認 `公務車借用管理` 欄位可見 |
| FT-003 | Office 365 Outlook 標準連接器基本連線 | 通過 | 已加入測試流程並成功執行 |
| FT-004 | 通知時間欄位支援 | 通過 | 已新增 `是否整天` 與 `預計通知時間` |
| FT-005 | SharePoint 系統防呆欄位建立與顯示 | 通過 | 已確認唯一識別、事件同步、卡片、通知、執行鎖、重複檢查、取消與失效欄位可見 |
| FT-006 | 資源行事曆 Calendar ID 可見性 | 未通過 | 2026-07-02 選單只有個人 `Calendar`，未列出三台公務車 |
| FT-007 | 以資源信箱 Email 讀取 Altis 行事曆 | 未通過 | `取得事件的行事曆檢視 (V3)` 回傳「ID 格式不正確」；Email 不是 Calendar ID |
| FT-008 | 測試流程停用 | 通過 | `公務車功能測試-資源信箱讀取` 已關閉，不再排程執行 |
| FT-009 | Calendar Reviewer 前置條件 | 部分通過 | `ad.general@alp.global` 可查看三台車行事曆且不需 Exchange Administrator；但 Reviewer 尚未使共用行事曆出現在 Power Automate Calendar ID 清單 |
| FT-010 | `取得行事曆 (V2)` 實際執行 | 部分通過 | 2026-07-02 15:34 成功執行，但輸出只有 `ad.general@alp.global` 的個人 `Calendar` |
| FT-011 | 測試排程安全關閉 | 通過 | V2 測試後已關閉流程，避免每分鐘重複執行 |
| FT-012 | Outlook 加入共用行事曆後重測 V2 | 未通過 | 2026-07-04 Outlook 已顯示三台公務車共用行事曆，但 `取得行事曆 (V2)` 仍只回傳一個個人 Calendar |

## 待執行測試案例

| 測試編號 | 測試情境 | 預期結果 | 狀態 |
|---|---|---|---|
| FT-101 | Outlook 建立一般公務車預約 | SharePoint 新增一筆借用紀錄 | 通過（測試紀錄 Id=4 首次同步成功建立） |
| FT-102 | Outlook 修改借用時間 | SharePoint 更新起訖時間，並重算 `預計通知時間` | 通過（2026-07-27 測試：Outlook 事件時間由 03:00-04:00 改為 10:00-11:00，SharePoint 正確更新起訖時間與預計通知時間，且與 v0.2.14 時區修正相容） |
| FT-103 | Outlook 取消預約 | SharePoint `事件同步狀態` 更新為 `已取消`、`已取消 = 是`，並停止後續通知 | 通過（v0.2.15）：新增事件取消偵測補強邏輯後，手動觸發測試，SharePoint 對應紀錄（Id=4）正確更新為 `事件同步狀態 = 已取消`、`已取消 = 是`，並寫入 `取消時間`；同時額外發現並正確標記一筆先前測試階段遺留的孤兒紀錄（Id=1），仍然有效的未來借用（Id=2、Id=3）未受影響。 |
| FT-104 | 借用時間為 09:30 | `預計通知時間` 為 08:30 | 通過（2026-07-30 於 ATA-9627 行事曆建立 09:30-10:30 測試事件，觸發 `公務車行事曆同步至SharePoint` 流程後，SharePoint 對應紀錄之預計通知時間正確計算為 08:30） |
| FT-105 | 借用時間為 08:30 | `預計通知時間` 為 08:00 | 通過（2026-07-31 於 ATA-9627 行事曆建立 08:30-09:30 測試事件，同步後 SharePoint 預計通知時間正確計算為 08:00） |
| FT-106 | 借用時間早於 08:00 | `預計通知時間` 為 08:00 | 通過（2026-08-01 於 ATA-9627 行事曆建立 07:30-08:30 測試事件，同步後 SharePoint 預計通知時間正確計算為 08:00，未低於下限） |
| FT-107 | 整天借用 | `是否整天` 為是，`預計通知時間` 為 08:00 | 通過（2026-08-02 於 ATA-9627 行事曆建立整天測試事件，同步後 SharePoint 是否整天=是，預計通知時間正確計算為 08:00） |
| FT-108 | 到達預計通知時間 | Teams Adaptive Card 發送給借用人 | 通過（手動觸發流程，測試紀錄成功收到 Teams 借用確認卡片，含車輛、借用日期、時間、使用事由與規範提示）。**v0.3.6 更新**：已補上「通知發送時間」欄位實際寫入邏輯，於送卡前鎖定動作（更新項目1）一併寫入 `@{utcNow()}`，與既有「通知狀態＝已通知」鎖定同一動作完成，流程儲存驗證零錯誤；精確發送時間戳將於下次排程實際發卡時人工抽查確認 |
| FT-109 | 借用人填寫共乘人數並勾選規範 | SharePoint 更新為 `已完成確認` | 通過（借用人於 Teams 卡片填入共乘人數並勾選規範確認後送出，SharePoint `領鑰狀態` 正確更新為 `已完成確認`，`共乘人數`、`規範確認狀態`、`Teams 回覆時間` 皆正確寫入） |
| FT-110 | 借用人未填共乘人數 | SharePoint 維持或更新為 `未完成填寫` | 通過（Adaptive Card 之共乘人數為必填欄位，未填寫時卡片端即擋下無法送出，等同確保 SharePoint 不會被未完整資料覆蓋） |
| FT-111 | 借用人未勾選規範 | SharePoint 維持或更新為 `未完成填寫` | 通過（Adaptive Card 之規範確認為必勾選項目，未勾選時卡片端即擋下無法送出，等同確保 SharePoint 不會被未完整資料覆蓋） |
| FT-112 | 承辦人查看後台 | 可依 `領鑰狀態` 判斷是否發放鑰匙 | 通過（透過 SharePoint REST API 查詢確認 領鑰狀態 欄位於所有正式紀錄皆正確填寫且可篩選，承辦人可據此判斷是否發放鑰匙） |
| FT-113 | 同一台車重複預約 | Outlook 資源信箱阻擋或避免雙重預約，或系統可標記異常供承辦人判斷 | 通過（v0.3.2）：不阻擋重複建立（保留改期等合理情境彈性），改於流程內新增同車時段重疊偵測邏輯，重疊時將標記標記為 `異常`；過程中發現並修正「建立項目」與「更新項目」時間格式不一致，導致標記於排程自動觸發後被靜默重置回 `正常` 的隱性 Bug；重新建立測試事件對驗證，確認標記可正確跨 Create→Update 轉換維持（一筆正常、一筆異常，Update 後互換但不會雙雙重置），詳見 `release-notes/v0.3.2.md`。**v0.3.6 更新**：原寫入共用的 `重複資料檢查結果` 欄位，已改為寫入新建的獨立欄位 `同車時段重疊檢查結果`，與 SG-T09 的判斷結果分離，不再互相覆蓋；既有 2 筆歷史異常紀錄（ID 20、22）已遷移至新欄位，通知排除條件已同步擴充比對新欄位，驗證流程儲存零錯誤 |
| FT-114 | 以 `取得行事曆 (V2)` 取得 ID 後逐台讀取 | Altis、Camry、Cross 均以真正 Calendar ID 讀到未來 7 天事件內容 | 受阻；Outlook 已顯示三台共用行事曆後，V2 仍只回傳個人 Calendar |

## 系統防呆測試案例（正式上線前必測）

| 測試編號 | 測試項目 | 測試方式 | 預期結果 | 狀態 |
|---|---|---|---|---|
| SG-T01 | 時區轉換測試 | 建立一般預約，檢查 Outlook 原始時間與 SharePoint 顯示時間 | SharePoint 顯示與通知判斷皆為 `Asia/Taipei`，不得直接使用 UTC | 通過（v0.2.14 已修正 Create/Update 時區位移問題並驗證） |
| SG-T02 | 全天事件測試 | 建立整天公務車預約 | `是否整天 = 是`，`預計通知時間 = 借用當天 08:00` | 通過（正式紀錄 Id=1 抽查確認：借用起始/結束時間、預計通知時間皆為 08:00，符合整天借用邏輯） |
| SG-T03 | 全天事件避免前一天通知 | 建立整天預約，檢查通知時間 | 不得產生前一天 23:00 或錯誤日期通知 | 通過（同 SG-T02 抽查證據，通知時間為借用當天 08:00，非前一天） |
| SG-T04 | 行事曆取消測試 | 取消 Outlook 預約 | SharePoint 更新為 `已取消`，後續 Teams 通知停止 | 通過（v0.2.15，同 FT-103）：新增取消偵測補強邏輯並驗證通過，詳見 `release-notes/v0.2.15.md`。 |
| SG-T05 | 行事曆異動測試 | 修改時間、車輛、主旨或借用人 | SharePoint 同步更新，重算 `預計通知時間`，舊卡片失效 | 通過，2026-08-08 完成獨立驗證（詳見下方說明）：時間異動、主旨異動皆已測試通過；車輛異動、借用人異動經評估後，結論如下——(1) 主旨異動：屬可獨立測試的真實「原地更新」情境，已取得負責人同意暫時開啟「公務車行事曆同步至SharePoint」流程完成端到端驗證：建立測試事件 `TEST-SGT05-S`（ATA-9627，2026/8/10 08:30-09:00）→ 手動觸發流程同步建立 SharePoint 紀錄（ID=26）→ 修改 Outlook 事件主旨為 `TEST-SGT05-S-已修改主旨` → 再次手動觸發流程 → 確認 SharePoint 紀錄之 Title／使用事由同步更新為修改後主旨、`事件同步狀態=有效`、Modified 時間戳更新為 2026-08-08T02:35:28Z，驗證通過後已將流程關閉，並清理測試用 SharePoint 紀錄（ID=26）與 Outlook 測試事件；(2) 車輛異動、借用人異動：結構上無法以「同一筆更新」方式測試——借用人對應 Outlook 事件之組織者（Organizer），車輛對應事件所在之資源行事曆，兩者於事件建立後皆無法直接編輯／更換，僅能取消舊事件並於另一資源行事曆重新建立，其行為已由 FT-101（新增）＋FT-103（取消）＋SG-T06（舊卡片失效）三項通過測試共同覆蓋，不另立測試案例 |
| SG-T06 | 舊卡片送出測試 | 使用者送出已取消或已異動案件的舊 Teams 卡片 | 不更新舊資料，提示「此借用已取消或已變更，請重新確認最新借用資訊。」 | 通過：正向（卡片資料與目前 SharePoint 紀錄一致）測試中，條件正確判定為符合，SharePoint 正確更新為 `已完成確認`；負向（卡片送出前，記錄的行事曆事件 ID 被異動，模擬卡片已過期）測試中，條件正確判定為不符合，Teams 正確顯示「此借用已取消或已變更，請重新確認最新借用資訊。」，且 SharePoint 未被更新（領鑰狀態、通知狀態等欄位皆維持異動前狀態），詳見 `release-notes/v0.3.0.md`。 |
| SG-T07 | 重複觸發測試 | 同一 Outlook 事件短時間內觸發兩次同步 | 不建立重複 SharePoint 紀錄，不重複通知 | **待重新驗證**（2026-08-27，v0.3.7）：原「通過」結論僅以『公務車行事曆同步至SharePoint』流程之重複觸發防護為依據；依 `docs/company-account-and-booking-rules-change-decisions.md` 決策，本測項需擴大為專門驗證『公務車借用前Teams通知與回覆』流程於 Bug#1（送卡鎖定＋Concurrency Control）修正後是否仍會重複發送 Teams 通知卡片，原結果撤回，待 #137 安排專門驗證後回填 |
| SG-T08 | Event ID 唯一鍵測試 | 用相同日期、主旨、借用人建立不同事件 | 系統以 Event ID / iCalUId 區分，不誤判為同一筆 | 通過（以正式紀錄佐證：兩筆商業發展處 ALP BD用車 紀錄主旨相同，但預約唯一鍵/Event ID 不同，SharePoint 正確區分為獨立兩筆，未誤判為同一筆） |
| SG-T09 | 重複資料異常測試 | 人為建立相同 `預約唯一鍵` 多筆資料 | 標記重複異常，不發送 Teams 通知 | 通過（v0.3.3）：於「公務車行事曆同步至SharePoint」新增條件2＋套用至各項3＋更新項目2，偵測同一 `預約唯一鍵` 筆數 >1 時將所有相符紀錄的 `重複資料檢查結果` 標記為 `疑似重複`；以人為建立的重複紀錄（TEST-SGT09-A／B，相同預約唯一鍵）驗證兩筆皆正確標記；並複核「公務車借用前Teams通知與回覆」流程之 8 項條件式，確認其中一項明確比對 `重複資料檢查結果 = 正常`，`疑似重複`／`異常`／其他值皆會被排除於通知對象之外，不發送 Teams 通知。詳見 `release-notes/v0.3.3.md`。**v0.3.6 更新**：`重複資料檢查結果` 欄位現已與 FT-113 的 `同車時段重疊檢查結果` 拆分為獨立欄位，本測項邏輯與判定結果不受影響，更新項目2 維持只寫入 `重複資料檢查結果` 一個欄位 |
| SG-T10 | Flow Concurrency 測試 | 同時同步多筆或重複事件 | 流程依序處理，不發生 Race Condition | 通過（v0.2.16）：Recurrence 觸發程序已啟用並行控制並設定平行處理原則的程度為 1，避免流程重疊執行；確認三個 Apply to each 迴圈預設即為循序執行。 |

## 測試資料原則

- 測試紀錄需勾選 `是否測試資料`。
- 測試完成後，`功能測試結果` 填入 `通過`、`未通過` 或 `待修正`。
- 測試資料不得與正式借用紀錄混淆。

## 測試通過標準

正式上線前，以下項目必須通過：

- 三台資源信箱皆可成功同步預約資料。
- 通知時間符合「借用前 1 小時，但最早 08:00」規則。
- 整天借用可於當天 08:00 通知。
- Teams Adaptive Card 可正確送達借用人。
- 借用人回覆可正確寫回 SharePoint。
- 未完整填寫時，`領鑰狀態` 不得變成 `已完成確認`。
- 承辦人可在 SharePoint 後台即時判斷是否發放鑰匙。
- 系統防呆測試 SG-T01 至 SG-T10 必須通過，才可正式上線。
## v0.2.7 測試案例 - ATA-9627 Resource Calendar V3 事件讀取

| 測試編號 | 測試項目 | 操作方式 | 預期結果 | 狀態 |
|---|---|---|---|---|
| CAL-V3-01 | Outlook Editor 權限確認 | 使用 `ad.general@alp.global` 開啟 Outlook 查看 ATA-9627 行事曆 | 可看到 ATA-9627 行事曆 | 通過 |
| CAL-V3-02 | Outlook 新增事件 | 在 ATA-9627 行事曆建立主旨 `TEST` 的事件 | 事件可建立成功 | 通過 |
| CAL-V3-03 | Outlook 修改事件 | 修改 `TEST` 事件 | 事件可修改成功 | 通過 |
| CAL-V3-04 | Outlook 刪除事件 | 刪除測試事件或測試刪除能力 | 事件可刪除成功 | 通過 |
| CAL-V3-05 | 取得行事曆 (V2) 列表確認 | 執行 `公務車功能測試-資源信箱讀取` | 僅列出 ad.general 自身 Calendar | 已確認 |
| CAL-V3-06 | V3 直接讀取事件 | 使用 Calendar Id `6049e1d1-b34c-4cca-b530-2c7c4b77abe9` 執行 `公務車功能測試-ATA9627事件讀取` | 可讀到 subject = TEST 或 ATA-9627 未來 7 天事件 | 未通過：`ErrorInvalidIdMalformed` |
| CAL-V3-07 | Event ID / iCalUId 確認 | 查看 V3 輸出欄位 | 可取得 id 與 iCalUId | 阻擋：V3 未取得事件 |
| CAL-V3-08 | Asia/Taipei 時區確認 | 比對 Outlook 顯示時間與流程輸出時間 | 顯示給人員看的時間為台北時間 | 阻擋：V3 未取得事件 |
| CAL-V3-09 | ATA-9627 候選 Calendar ID 驗證 | 將候選值送入 V3 並記錄執行輸入 | Calendar ID 可供 V3 讀取事件 | 未通過：候選 GUID 不是 V3 可接受 ID |
| CAL-V3-10 | ATA-9627 V3 測試實作版 | 依 `power-automate/ata9627-v3-test-implementation.md` 建立測試動作與輸出 | 測試動作、Expression 與成功判斷已明確 | 文件完成，待實測 |
| CAL-V3-11 | V3 錯誤證據保存 | 保存 Flow、Run、Request 與 Tracking ID | 可重現並追蹤本次連接器錯誤 | 通過：見 `docs/ata9627-v3-calendar-event-test.md` |
